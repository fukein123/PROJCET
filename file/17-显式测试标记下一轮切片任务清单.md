# 显式测试标记下一轮切片任务清单

更新时间：2026-03-13

## Scope

- Baseline: `file/16-结构治理下一轮切片任务清单.md`
- Objective: 围绕“社区志愿服务管理”核心业务，继续处理内容模块与自动化链路中的残留结构问题，优先消除测试数据对业务评论视图的污染。
- Current Status:
  - `G1` 已完成
  - `G2` 已完成
  - `G3` 已完成
  - `G4` 已完成

## Tasks

- [x] G1. 评论测试数据改为显式标记
  - File: `backend/src/main/resources/db/schema.sql`
  - File: `backend/src/main/resources/db/data.sql`
  - File: `backend/src/main/java/com/community/modules/content/controller/ContentController.java`
  - File: `backend/src/main/java/com/community/modules/content/service/ContentService.java`
  - File: `backend/src/main/java/com/community/modules/content/service/ContentQueryService.java`
  - File: `Frontend/src/utils/request.ts`
  - File: `Frontend/scripts/smoke-flow.mjs`
  - File: `Frontend/src/views/content/ContentManageView.vue`
  - 用显式 `test_data_tag` 替代旧的评论关键字语义；评论创建仅通过请求头注入测试标记，普通评论分页默认隔离测试数据，管理端可显式查看并清理自动化残留。
  - Purpose: 消除“靠文案关键字区分测试数据”的脆弱实现，避免污染门户端与志愿者端评论视图。
  - Validation: `mvn -DskipTests compile`, `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test`, `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts`, `npm run test:unit`, `npm run build`, `npm run smoke:check`

- [x] G2. 收藏记录补充活动快照字段
  - File: `backend/src/main/resources/db/schema.sql`
  - File: `backend/src/main/java/com/community/modules/content/*`
  - File: `Frontend/src/views/volunteer/VolunteerMyFavoritesView.vue`
  - 为收藏记录增加活动标题、地点、时间快照字段；创建收藏时固化快照，旧记录通过初始化脚本回填，收藏页优先展示快照信息。
  - Purpose: 提升志愿者收藏记录的历史可读性与稳定性。
  - Validation: `mvn -DskipTests compile`, `mvn "-Dtest=ContentControllerContractIT,ContentCommandServiceTest" test`, `npm run test:unit`, `npm run build`, `npm run smoke:check`

- [x] G3. 管理端补充批量归档/恢复能力
  - File: `backend/src/main/java/com/community/modules/activity/controller/ActivityController.java`
  - File: `backend/src/main/java/com/community/modules/activity/service/ActivityQueryService.java`
  - File: `backend/src/main/java/com/community/modules/activity/service/ActivityCommandService.java`
  - File: `backend/src/main/java/com/community/modules/content/controller/ContentController.java`
  - File: `backend/src/main/java/com/community/modules/content/service/ContentCommandService.java`
  - File: `Frontend/src/api/activity.ts`
  - File: `Frontend/src/api/content.ts`
  - File: `Frontend/src/views/activity/ActivityManageView.vue`
  - File: `Frontend/src/views/content/ContentManageView.vue`
  - 活动新增独立 `ARCHIVED` 状态与批量归档/恢复接口，管理员活动分页显式支持 `includeArchived=true` 查看已归档记录；动态与公告复用现有 `1/0` 发布状态完成批量归档/恢复，志愿者端与门户端默认继续隔离已归档活动。
  - Purpose: 提升社区运营后台的治理稳定性，在不误伤历史内容的前提下提供比批量删除更稳妥的收口路径。
  - Validation: `mvn -DskipTests compile`, `mvn "-Dtest=ActivityControllerContractIT,ActivityCommandServiceTest,ContentControllerContractIT,ContentCommandServiceTest" test`, `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts src/views/activity/__tests__/ActivityManageView.spec.ts`, `npm run test:unit`, `npm run build`, `npm run smoke:check`

- [x] G4. 初始化 SQL 迁移治理预研
  - File: `backend/src/main/resources/db/*.sql`
  - File: `backend/src/main/resources/db/migration/*.sql`
  - File: `backend/src/main/resources/application*.yml`
  - File: `backend/pom.xml`
  - 后端启动从 `spring.sql.init` 切换到 `Flyway`；建立 `V1__baseline_schema.sql` 与 `R__seed_reference_data.sql` 迁移基线，保留 `init-database.sql` 作为数据库/账号的手工引导脚本，原 `schema.sql` / `data.sql` 退役为占位说明，避免继续在根目录堆叠可执行初始化逻辑。
  - Purpose: 降低本地 SQL 脚本与后续部署环境之间的漂移风险，为多环境交付建立稳定的迁移入口。
  - Validation: `mvn -DskipTests compile`, `mvn "-Dtest=ActivityControllerContractIT,ContentControllerContractIT" test`

## Execution Order

1. `G1 -> G2 -> G3 -> G4`

## Exit Criteria

- 评论测试数据不再依赖关键字文案识别。
- 普通业务评论查询默认不受自动化残留污染。
- 管理端对测试评论具备明确识别与清理能力。
- 下一轮结构治理任务继续保持单切片推进。
