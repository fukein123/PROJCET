# 平台完善记录

更新时间：2026-03-13

## 本轮完成事项

1. 登录与注册
- 注册页重排，提升信息结构可读性。
- 注册页支持头像上传。
- 登录失败后验证码会刷新。
- 门户退出后正确显示“登录”按钮。

2. 去积分化
- 移除门户积分入口。
- 管理员看板移除兑换统计，替换为评论统计。

3. 管理端能力增强
- 活动分类支持删除与批量删除。
- 活动管理支持封面上传、删除与批量删除。
- 内容管理（动态/公告/帖子/评论）支持删除与批量删除。
- 动态发布支持图片上传。

4. 志愿者端能力增强
- 活动中心和门户活动页展示更详细活动信息。
- 打卡记录展示服务时长、签到签退距离等关键信息。
- 我的收藏升级为完整 CRUD（新增、查询、编辑、删除、批量删除）。

5. 主题与视觉
- 管理员端与志愿者端采用差异化主题色。
- 网站图标替换为社区志愿服务主题。

6. 自动化与文档
- 冒烟脚本覆盖登录、报名、评论、退出。
- 评论文案调整，不再写入“自动化冒烟评论”。
- 已更新 `file/` 目录相关文档，并补充深度重构观察报告。

7. 本轮追加优化（zero2.0 补充）
- 注册页按“账号→密码→确认密码→姓名→头像→电话→邮箱→性别”重排。
- 注册页头像上传交互重做为“+”样式上传框，并强化志愿服务宗旨文案。
- 志愿者周统计页重写，支持按周查询与重建统计，展示统计口径说明。
- 用户管理升级为志愿者完整 CRUD（新增、查询、编辑、删除、批量删除）。
- 活动管理新增必填字段：`活动内容`、`志愿者人数`，并保留 `目标人数`、`开始/结束时间`、`详细描述`。
- 活动中心支持进入活动详情页，展示活动信息、详细说明与活动评价。
- 移除活动经纬度与打卡定位依赖，修复活动新增“请求参数格式不正确”高频问题。
- 审核页“通过/拒绝”改为强视觉按钮，补齐活动名、账号、姓名，提升判断准确性。
- 后端分页改为 PageHelper 统一处理，覆盖管理员端与志愿者端关键分页列表。

8. 结构治理（2026-03-12 第二阶段）
- 前端抽出 `useTable.ts` 统一分页查询、翻页和重置逻辑。
- 前端新增 `utils/display.ts`，统一活动状态、报名状态、打卡状态、时间格式化与服务时长展示。
- 前端新增 `utils/upload.ts`，统一图片上传前置校验。
- 删除未使用的 `Frontend/src/components/AppTable.vue` 与无效状态字段 `themeName`。
- 文档、部署配置与 `.gitignore` 中旧的 `admin/` 路径统一改为 `Frontend/`。
- 项目内 `skills/` 缩减为核心 skill，减少仓库冗余内容。

9. 规格流程一致性现状（2026-03-12 历史记录校正）
- 历史文档曾把 `.spec-workflow/specs/community-volunteer-service-platform/` 记作已存在实例，但当前工作树中并未看到这套正式目录。
- 本轮已把正式规格基线恢复到 `.spec-workflow/specs/community-volunteer-service-platform/requirements.md`。
- 在正式 requirements 完成当前本地审批前，不进入 `tasks.md` 和正式 spec implementation 阶段。

10. 第三阶段结构治理补充（2026-03-13）
- 删除与当前社区志愿服务平台主工程无关、且被 Git 跟踪的 `photo-organizer/` 和 `zero/` Speckit 模板副本。
- 新增 `Frontend/src/composables/useSelectionIds.ts`，统一表格多选 ID 收集与清空逻辑。
- 新增 `Frontend/src/utils/confirmed-action.ts`，统一高风险操作的确认提示、成功提示与后置刷新。
- `ActivityCategoryView`、`ActivityManageView`、`UserManageView`、`VolunteerMyFavoritesView` 已切换到共享选择/确认能力。
- `VolunteerMyFavoritesView` 已重新接入 `useTable` 公共分页能力，减少手写分页状态。
- `ContentManageView` 改为按标签页加载与按模块刷新，避免初始化与保存后无差别拉取四组数据。
- `Frontend/package-lock.json` 顶层包名已从旧值刷新为 `zero2-frontend`。
- 本轮验证完成：
  - 前端 `npm run build`
  - 后端 `mvn -DskipTests compile`
  - 冒烟脚本 `npm run smoke`，结果为“登录成功 / 报名返回已报名幂等提示 / 评论发布成功 / 退出成功”

11. 正式规格基线恢复与本地审批收口（2026-03-13）
- 已创建正式规格需求文档：`.spec-workflow/specs/community-volunteer-service-platform/requirements.md`。
- 已建立仓库内本地审批工具：
  - 脚本：`scripts/local-approval.ps1`
  - 记录目录：`.approval-workflow/`
- 已创建本地 requirements 审批请求：`local_approval_20260313010858606_pxfklr6z`
- 新审批规则改为：用户在聊天中直接回复 `通过` 或 `需修改：...`，由助手写入本地审批记录。
- 已完成本地审批工具自检：`request -> approve -> delete` 链路通过。
- 当前本地审批状态仍为 `pending`；在用户明确回复 `通过` 前，不进入 design、tasks 或正式 spec implementation 阶段。

12. 治理规则强制切换到聊天审批（2026-03-13）
- 已同步修改 `.specify/memory/constitution.md`，明确本仓库审批只认“用户聊天回复 + `scripts/local-approval.ps1` 本地落档”。
- 已同步修改 `.specify/templates/spec-template.md`、`.specify/templates/plan-template.md`、`.specify/templates/tasks-template.md`，确保后续代理只沿本地聊天审批链路推进。
- 当前正式 requirements 审批门禁保持不变，仍等待用户聊天中明确回复 `通过` 或 `需修改：...`。

13. 旧审批与旧草案链路清理（2026-03-13）
- 已删除根目录 `specs/001-workflow-recovery/` 与 `specs/002-community-volunteer-platform/` 非正式草案目录。
- 已清理项目文档中的旧草案入口与旧审批动作映射，只保留 `.spec-workflow/specs/` 与 `.approval-workflow/ + scripts/local-approval.ps1`。
- 已清理本地审批自检残留，仅保留正式在用审批记录。

14. requirements 审批通过并切换至 design 阶段（2026-03-13）
- 用户已在聊天中明确回复 `通过`，助手已写入本地审批结果。
- requirements 审批单 `local_approval_20260313010858606_pxfklr6z` 已从 `pending` 更新为 `approved`，并归档到：
  - `.approval-workflow/archive/community-volunteer-service-platform/local_approval_20260313010858606_pxfklr6z.json`
- 已创建设计文档：
  - `.spec-workflow/specs/community-volunteer-service-platform/design.md`
- 已创建 design 审批单：
  - `local_approval_20260313080841916_pkht1nuo`
- 当前阶段门禁已切换为 design 审批；在 design 审批通过并清理前，不进入 `tasks.md` 阶段。

15. design 审批通过并切换至 tasks 阶段（2026-03-13）
- 用户已在聊天中明确回复 `通过`，助手已写入 design 本地审批结果。
- design 审批单 `local_approval_20260313080841916_pkht1nuo` 已从 `pending` 更新为 `approved`，并归档到：
  - `.approval-workflow/archive/community-volunteer-service-platform/local_approval_20260313080841916_pkht1nuo.json`
- 已创建任务文档：
  - `.spec-workflow/specs/community-volunteer-service-platform/tasks.md`
- 已创建 tasks 审批单：
  - `local_approval_20260313081633315_uqictn9b`
- 当前阶段门禁已切换为 tasks 审批；在 tasks 审批通过并清理前，不进入正式 implementation 阶段。

16. tasks 审批通过并进入 implementation（2026-03-13）
- 用户已在聊天中明确回复 `通过`，助手已写入 tasks 审批结果并完成归档。
- tasks 审批单 `local_approval_20260313081633315_uqictn9b` 已归档到：
  - `.approval-workflow/archive/community-volunteer-service-platform/local_approval_20260313081633315_uqictn9b.json`
- 已进入 implementation，并完成任务 1（登录角色匹配与登出状态一致性）：
  - 后端 `AuthService` 统一登录失败提示为“账号、密码或登录入口错误”，并保留角色匹配校验。
  - 前端 `userStore` 新增角色归一化、统一登录态清空方法，以及登录后二次角色一致性校验。
  - 前端路由守卫新增非法 `token + role` 组合的自动清理与重定向。
- 任务状态与日志：
  - `.spec-workflow/specs/community-volunteer-service-platform/tasks.md` 任务 1 已从 `[-]` 更新为 `[x]`
  - implementation log 已写入（taskId=1）
- 本轮验证：
  - 后端：`mvn -DskipTests compile`
  - 前端：`npm run build`

17. implementation 任务 2 完成（2026-03-13）
- 任务：活动分类删除约束回归与边界修正。
- 代码调整：
  - `backend/src/main/java/com/community/modules/activity/service/ActivityService.java`
  - `batchDeleteCategories` 增加空值过滤后的去重处理（`distinct()`）。
- 测试补齐：
  - 新增 `backend/src/test/java/com/community/modules/activity/ActivityServiceCategoryTest.java`
  - 覆盖场景：
    - 分类被活动引用时单删失败
    - 分类未被引用时单删成功
    - 批量删除跳过空值并去重
    - 批量删除遇到被引用分类时抛错中断
- 任务状态与日志：
  - `.spec-workflow/specs/community-volunteer-service-platform/tasks.md` 任务 2 已更新为 `[x]`
  - implementation log 已写入（taskId=2）
- 验证：
  - 后端定向测试：`mvn -Dtest=ActivityServiceCategoryTest test` 通过

18. implementation 任务 3 与任务 4 完成（2026-03-13）
- 任务 3：活动查询职责拆分
  - 新增 `backend/src/main/java/com/community/modules/activity/service/ActivityQueryService.java`
  - 迁移查询能力：活动分页/详情、报名分页、签到记录分页
  - `ActivityService` 查询方法改为门面委托
  - implementation log：taskId=3
- 任务 4：活动命令职责拆分
  - 新增 `backend/src/main/java/com/community/modules/activity/service/ActivityCommandService.java`
  - 迁移写路径：活动创建/更新/删除、报名、审核、签到签退、过期结束
  - `ActivityService` 写路径方法改为门面委托
  - 为适配新依赖，`ActivityServiceCategoryTest` 补充 `ActivityQueryService`/`ActivityCommandService` mock
  - implementation log：taskId=4
- 验证：
  - 后端编译：`mvn -DskipTests compile` 通过
  - 回归测试：`mvn -Dtest=ActivityServiceCategoryTest test` 通过

19. implementation 任务 5 完成（2026-03-13）
- 任务：细化论坛审核能力边界。
- 新增服务：
  - `backend/src/main/java/com/community/modules/content/service/ForumModerationService.java`
  - 统一处理帖子审核状态机，限制仅 `PENDING` 帖子可审核到 `APPROVED/REJECTED`。
  - 对 `REJECTED` 状态强制要求审核意见，避免无理由驳回。
- 改造点：
  - `backend/src/main/java/com/community/modules/content/service/ContentService.java`
  - `auditPost` 改为委托 `ForumModerationService`，保持控制器接口不变。
- 任务状态与日志：
  - `.spec-workflow/specs/community-volunteer-service-platform/tasks.md` 任务 5 已更新为 `[x]`
  - implementation log 已写入（taskId=5）
- 验证：
  - 后端编译：`mvn -DskipTests compile` 通过

20. implementation 任务 6 完成（2026-03-13）
- 任务：统一管理端剩余操作反馈链路。
- 共享工具增强：
  - `Frontend/src/utils/confirmed-action.ts`
  - 新增可配置项：确认按钮文案、取消按钮文案、提示类型。
- 页面收敛：
  - `Frontend/src/views/audit/ApplicationAuditView.vue`
  - 审核通过/拒绝统一走共享确认工具；拒绝流程先收集理由，再统一确认、提示与刷新。
  - `Frontend/src/views/volunteer/VolunteerActivityCenterView.vue`
  - 报名/收藏统一走共享确认工具，并在成功后刷新当前分页列表；表格补上 `loading` 反馈。
- 任务状态与日志：
  - `.spec-workflow/specs/community-volunteer-service-platform/tasks.md` 任务 6 已更新为 `[x]`
  - implementation log 已写入（taskId=6）
- 验证：
  - 前端构建：`npm run build` 通过

21. implementation 任务 7 与任务 8 完成（2026-03-13）
- 任务 7：内容管理分页与按标签加载前端回归测试
  - 新增前端单元测试基础设施，补齐 `vitest + @vue/test-utils + jsdom`
  - 新增 `Frontend/src/views/content/__tests__/ContentManageView.spec.ts`
  - 覆盖首屏仅加载当前标签页、帖子审核后只刷新帖子标签两类回归场景
  - implementation log 已写入（taskId=7）
- 任务 8：评论分页与目标过滤后端集成测试
  - 新增 `backend/src/test/java/com/community/modules/content/ContentControllerCommentIT.java`
  - 覆盖公开场景下 `targetType + targetId` 组合过滤、`targetType` 单独过滤、`targetId` 缺失目标类型校验、匿名 `onlyMine=true` 拒绝、登录后 `onlyMine=true` 放行
  - `backend/src/main/java/com/community/modules/content/controller/ContentController.java` 增加评论分页请求边界校验，避免匿名“我的评论”查询被静默放大为全量公开查询
  - implementation log 已写入（taskId=8）
- 验证：
  - 前端单测：`npm run test:unit -- ContentManageView.spec.ts` 通过
  - 前端单测全量：`npm run test:unit` 通过
  - 后端定向测试：`mvn -Dtest=ContentControllerCommentIT test` 通过

22. implementation 任务 9 至任务 12 收口（2026-03-13）
- 任务 9：周榜重建与定时任务可观测性
  - `VolunteerWeeklyStatsService` 新增带触发源的重建日志、读修复日志与事务保护。
  - `ActivityScheduleTask` 补齐定时任务开始/成功/失败日志与耗时信息。
  - `DashboardController` 手动重建入口改为显式传递 `manual-api` 触发源。
  - implementation log 已写入（taskId=9）
- 任务 10：自动化冒烟扩展到审核、签到签退与周统计重建
  - `Frontend/scripts/smoke-flow.mjs` 改为“API 预置临时活动 + 浏览器走报名/审核/签到签退/周统计重建/退出”的混合链路。
  - `Frontend/package.json` 新增 `npm run smoke:check`，用于在本地服务未启动时先做脚本语法校验。
  - `file/11-自动化冒烟脚本说明.md` 已同步更新为新链路说明。
  - implementation log 已写入（taskId=10）
- 任务 11：需求到接口映射文档
  - `file/02-接口文档.md` 新增 Requirement 1-6 对 API / 页面 / 服务 / 治理资产的总览矩阵。
  - `file/08-新增接口与数据结构说明.md` 新增“Requirement -> 本轮新增/扩展接口 -> 页面/服务”细化矩阵。
  - implementation log 已写入（taskId=11）
- 任务 12：治理文档刷新与规格闭环
  - 已刷新 `file/06-平台完善记录.md`、`file/12-阶段治理与迭代计划.md`、`file/13-开发约束与计划跟踪.md`。
  - `.spec-workflow/specs/community-volunteer-service-platform/tasks.md` 当前 12 项任务已全部标记为 `[x]`。
- 验证：
  - 后端编译：`mvn -DskipTests compile` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟静态校验：`npm run smoke:check` 通过
  - 冒烟实跑：`npm run smoke` 已尝试，但因本地后端未启动（`127.0.0.1:8080` refused）未完成 live 验证

23. 长期开发准则固化（2026-03-13）
- 已将后续固定开发准则沉淀到治理文档与 constitution：
  - 严格按 requirements / design 分段开发，一次一个功能切片
  - 严格前后端分离，保持社区志愿服务管理为唯一设计核心
  - 优先使用少量高质量 skill，必要时再补装
  - 统一系统 UI 语言并持续执行代码/目录治理
  - 每轮实现后同步更新文档、汇报进度与下一步方向，并在用户要求阶段确认时先等待确认

24. 统一风格升级切片 A1 完成（2026-03-13）
- 新增前端设计令牌基线：
  - `Frontend/src/assets/styles/tokens.css`
  - `Frontend/src/assets/styles/index.css`
- 将全局样式入口从 `theme.css` 收敛为 `index.css -> tokens.css + theme.css`，保留现有视觉方向并补齐字号、间距、圆角、阴影、动效时长等统一变量。
- `Frontend/src/assets/styles/theme.css` 已改为消费设计令牌，不再自行声明散落的根变量。
- `Frontend/src/components/SearchForm.vue` 已切换为令牌驱动的圆角和间距，作为共享组件先行接入。
- 任务清单 `file/14-统一风格前后端分离任务清单.md` 中 A1 已标记为完成。
- 验证：
  - 前端构建：`npm run build` 通过
  - Playwright 本地预览验收已尝试启动，但本机缺少可用 Chromium，浏览器安装在当前环境中超时，未完成页面快照验证

25. 统一风格升级切片 A3 完成（2026-03-13）
- 新增 Element Plus 主题覆盖层：
  - `Frontend/src/assets/styles/element-overrides.css`
- 将 Element Plus 全局设计变量映射到现有社区志愿服务设计令牌，统一以下基础交互外观：
  - 主色、成功/警告/危险/信息语义色
  - 全局字体、边框、背景、阴影、圆角
  - 按钮、输入框、选择器、日期控件、分页、表格、弹窗/抽屉
- `Frontend/src/assets/styles/index.css` 已接入 `element-overrides.css`，覆盖顺序固定为 `tokens -> element-overrides -> theme`。
- `Frontend/src/main.ts` 已明确样式加载职责，保证覆盖层在 Element Plus 默认样式之后生效。
- 任务清单 `file/14-统一风格前后端分离任务清单.md` 中 A3 已标记为完成。
- 验证：
  - 前端构建：`npm run build` 通过

26. 统一风格升级切片 A2 完成（2026-03-13）
- 已将前端构建基线升级到 npm registry 当前稳定版本：
  - `vite`：`^7.3.1` -> `^8.0.0`
  - `@vitejs/plugin-vue`：`^6.0.4` -> `^6.0.5`
- 已刷新：
  - `Frontend/package.json`
  - `Frontend/package-lock.json`
- `vite.config.ts` 在本轮升级下无需额外兼容性改动，现有构建与测试配置保持可用。
- 验证：
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过
- 附注：
  - `npm install` 过程中出现旧 `esbuild` / `rollup` 临时目录清理 `EPERM` 警告，但未影响依赖升级结果与后续构建、测试通过

27. 统一风格升级切片 B1 完成（2026-03-13）
- 新增管理端列表统一骨架组件：
  - `Frontend/src/components/admin/AdminListScaffold.vue`
- `Frontend/src/components/SearchForm.vue` 已升级为统一筛选栏布局：
  - 固定“筛选字段区 + 查询/重置操作区”结构
  - 支持换行与移动端自适应
  - 回车提交直接触发查询
- 已接入统一骨架的代表页面：
  - `Frontend/src/views/activity/ActivityCategoryView.vue`
  - `Frontend/src/views/user/UserManageView.vue`
  - `Frontend/src/views/audit/ApplicationAuditView.vue`
- 本轮统一收敛了三类管理页形态：
  - 仅头部操作
  - 筛选 + 头部操作 + 分页
  - 筛选 + 分页
- 验证：
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过

28. 统一风格升级切片 B3 完成（2026-03-13）
- 新增共享导航与页面状态组件：
  - `Frontend/src/components/shared/BrandMark.vue`
  - `Frontend/src/components/shared/WorkspaceHero.vue`
  - `Frontend/src/components/shared/StatePanel.vue`
- 门户端导航与页面层级统一：
  - `PortalNavBar.vue` 改为统一社区志愿服务品牌头部，风格从分裂的红色导航收敛到社区服务主视觉。
  - `PortalHomeView.vue`、`PortalActivitiesView.vue`、`PortalForumView.vue`、`PortalNewsView.vue`、`PortalNoticesView.vue` 已接入共享页头与空态/加载态。
- 志愿者端导航与页面层级统一：
  - `MainLayout.vue` 接入统一品牌标识与工作台头部信息。
  - `VolunteerHomeView.vue`、`VolunteerActivityCenterView.vue`、`VolunteerActivityDetailView.vue` 已接入共享页头与空态/加载态。
- 本轮同时完成若干页面文案与结构清理：
  - 修复门户/志愿者核心页面中的乱码文案风险。
  - 收敛重复的 hero、模块头部、空列表提示与加载提示写法。
- 验证：
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过

29. 统一风格升级切片 B2 完成（2026-03-13）
- 共享确认工具增强：
  - `Frontend/src/utils/confirmed-action.ts` 新增 `prompt` 模式，支持“输入理由/意见 -> 提交动作 -> 成功提示 -> 刷新”的统一链路。
- 已完成页面收敛：
  - `Frontend/src/views/audit/ApplicationAuditView.vue`
    - 拒绝报名申请改为共享确认工具驱动，不再手写 `ElMessageBox.prompt`。
  - `Frontend/src/views/content/ContentManageView.vue`
    - 帖子审核通过/拒绝统一接入 `runConfirmedAction`，拒绝时必须填写原因。
  - `Frontend/src/views/volunteer/VolunteerActivityDetailView.vue`
    - 报名活动、加入收藏已与 `VolunteerActivityCenterView` 对齐为统一确认与成功反馈。
- 测试同步：
  - `Frontend/src/views/content/__tests__/ContentManageView.spec.ts` 已更新共享确认工具 mock，确保现有回归测试保持有效。
- 验证：
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过

30. 前后端契约切片 C1 完成（2026-03-13）
- 后端统一错误码契约：
  - 新增 `backend/src/main/java/com/community/common/exception/ApiErrorCode.java`
  - 统一定义参数错误、未登录、无权限、资源不存在、业务冲突、校验失败、系统异常等标准业务码与 HTTP 状态映射。
- 后端异常处理与安全入口统一：
  - `BusinessException` 改为兼容旧整数构造，同时映射到标准错误码与真实 HTTP 状态。
  - `GlobalExceptionHandler` 改为返回真实 HTTP 状态，不再固定 `200 + body.code`。
  - `SecurityConfig` 的未登录/无权限响应已改为统一错误码输出。
- 前端请求层统一提示模板：
  - `Frontend/src/utils/request.ts` 已按错误码分类处理：
    - 未登录/登录失效
    - 无权限
    - 参数错误与校验失败
    - 资源不存在
    - 业务冲突
    - 系统异常与网络异常
  - 登录失效时会统一清理本地鉴权信息并跳回登录页。
- 测试与回归：
  - `backend/src/test/java/com/community/modules/content/ContentControllerCommentIT.java`
    - 已从旧的 `status=200 + body.code=400/401` 断言更新为真实 HTTP 状态与标准业务码断言。
- 验证：
  - 后端定向测试：`mvn -Dtest=ContentControllerCommentIT test` 通过
  - 后端编译：`mvn -DskipTests compile` 通过
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过

31. 前后端契约切片 C2 完成（2026-03-13）
- 新增高频接口契约回归测试：
  - `backend/src/test/java/com/community/modules/activity/ActivityControllerContractIT.java`
  - `backend/src/test/java/com/community/modules/content/ContentControllerContractIT.java`
- 活动模块已固定四类成功态契约：
  - 列表：`GET /api/activity/categories`
  - 分页：`GET /api/activity/page`
  - 详情：`GET /api/activity/{id}`
  - 提交：`POST /api/activity/{activityId}/apply`
- 内容与收藏模块已固定三类高频契约：
  - 列表：`GET /api/content/forum/categories`、`GET /api/content/favorites`
  - 分页：`GET /api/content/forum/posts/page`、`GET /api/content/favorites/page`
  - 提交：`POST /api/content/favorites`
- 本轮额外核对 `Frontend/src/api/activity.ts` 与 `Frontend/src/api/content.ts`：
  - 当前 API 路径、分页结构类型 `PageResult<T>`、成功返回数据拆包方式与后端保持一致
  - 本切片无需新增前端兼容层改动
- 验证：
  - 后端定向测试：`mvn -Dtest=ActivityControllerContractIT,ContentControllerContractIT,ContentControllerCommentIT test` 通过
  - 后端编译：`mvn -DskipTests compile` 通过
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过

32. 统一风格升级切片 D1 完成（2026-03-13）
- `Frontend/scripts/smoke-flow.mjs` 已扩展为完整关键链路冒烟：
  - 登录
  - 报名
  - 评论
  - 审核
  - 签到
  - 签退
  - 周统计重建
  - 批量删除分类
  - 退出
- 本轮新增“批量删除分类”回归：
  - 先通过后端 API 预置两个临时活动分类
  - 再在管理端 `ActivityCategoryView` 页面中完成多选、确认弹窗、批量删除与结果校验
  - 同时覆盖统一列表骨架、多选列、危险操作确认和成功反馈
- 冒烟脚本新增临时数据清理：
  - 自动清理临时活动
  - 自动清理未成功删除的临时分类
- 修复冒烟脚本连续读取 toast 时的误判问题，避免将上一条“签到成功”误读为签退结果。
- 已更新 `file/11-自动化冒烟脚本说明.md`，同步说明批量删除链路和临时数据清理策略。
- 验证：
  - 冒烟脚本静态校验：`npm run smoke:check` 通过
  - 冒烟实跑：`npm run smoke` 通过

33. 统一风格升级切片 D2 完成（2026-03-13）
- 新增 `scripts/generate_ui_manual.py`，将当前统一风格实现固化为可重复生成的文档产物。
- 已生成并落盘：
  - `file/14-1-UI规范手册.md`
  - `file/14-2-UI规范手册.pdf`
- 手册内容已覆盖统一风格执行所需的关键维度：
  - 设计核心与角色分区色带
  - 设计令牌：色板、字体层级、间距、圆角、动效、阴影
  - 共享组件：`BrandMark`、`WorkspaceHero`、`StatePanel`、`SearchForm`、`AdminListScaffold`
  - 交互规范、页面模板、响应式断点与实现映射
- PDF 生成链路已内置校验：
  - `pypdf` 校验页数不少于 4 页
  - `pdfplumber` 校验关键文本 `UI / 规范 / 社区志愿服务平台 / 设计原则`
  - `PyMuPDF` 校验全部页面可成功渲染为预览图
- 任务清单 `file/14-统一风格前后端分离任务清单.md` 中 D2 已标记为完成。
- 验证：
  - `python scripts/generate_ui_manual.py` 通过

34. 统一风格升级切片 D3 完成（2026-03-13）
- 已新增验收报告：
  - `file/14-3-统一风格验收报告.md`
- 报告已汇总以下内容：
  - 统一风格升级已完成切片清单
  - 前后端分离与设计核心的验收结论
  - 本轮实际复验结果
  - 交付物清单
  - 遗留风险与下一轮迭代建议
- 本轮实际复验结果：
  - 后端编译：`mvn -DskipTests compile` 通过
  - 后端契约回归：`mvn "-Dtest=ActivityControllerContractIT,ContentControllerContractIT,ContentControllerCommentIT" test` 通过，`14` 个测试全部通过
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过
  - UI 手册生成：`python scripts/generate_ui_manual.py` 通过
- 环境说明：
  - 本轮未重跑 `npm run smoke`，因为验收时本地 `127.0.0.1:5173` 与 `127.0.0.1:8080` 未监听
  - 保留 D1 当日 live 冒烟通过记录，但未把历史结果记作本轮复验
- 任务清单 `file/14-统一风格前后端分离任务清单.md` 中 D3 已标记为完成。

35. 下一轮统一风格切片 E1 完成（2026-03-13）
- 新增志愿者端共享内容壳组件：
  - `Frontend/src/components/volunteer/VolunteerPageSection.vue`
- 已统一收口以下志愿者长尾页面：
  - `Frontend/src/views/volunteer/VolunteerProfileView.vue`
  - `Frontend/src/views/volunteer/VolunteerApplyRecordsView.vue`
  - `Frontend/src/views/volunteer/VolunteerCheckRecordsView.vue`
  - `Frontend/src/views/volunteer/VolunteerMyFavoritesView.vue`
  - `Frontend/src/views/volunteer/VolunteerMyPostsView.vue`
  - `Frontend/src/views/volunteer/VolunteerMyCommentsView.vue`
- 本轮统一内容：
  - 接入 `WorkspaceHero`
  - 接入 `VolunteerPageSection`
  - 接入 `StatePanel` loading / empty 状态
  - 为“我的帖子”“我的评论”补齐分页
  - 新增帖子状态与评论目标类型共享显示映射
- 已完成浏览器 spot check：
  - `Frontend/output/playwright/volunteer-profile.png`
  - `Frontend/output/playwright/volunteer-my-posts.png`
- 已新增下一轮任务清单：
  - `file/15-统一风格下一轮细化任务清单.md`
- 验证：
  - 后端编译：`mvn -DskipTests compile` 通过
  - 前端构建：`npm run build` 通过
  - 前端单测：`npm run test:unit` 通过
  - 冒烟脚本静态校验：`npm run smoke:check` 通过
  - 冒烟实跑：`npm run smoke` 通过

36. 统一风格下一轮细化 E2-E5 完成（2026-03-13）
- 已新增视觉快照基线脚本：
  - `Frontend/scripts/visual-baseline.mjs`
- 已输出门户端与志愿者端关键页面截图基线：
  - `Frontend/output/playwright/baseline/portal-home.png`
  - `Frontend/output/playwright/baseline/portal-activities.png`
  - `Frontend/output/playwright/baseline/portal-forum.png`
  - `Frontend/output/playwright/baseline/volunteer-home.png`
  - `Frontend/output/playwright/baseline/volunteer-activity-center.png`
  - `Frontend/output/playwright/baseline/volunteer-profile.png`
  - `Frontend/output/playwright/baseline/volunteer-my-posts.png`
  - `Frontend/output/playwright/baseline/manifest.json`
- 已补齐志愿者长尾自助页视图级测试：
  - `Frontend/src/views/volunteer/__tests__/VolunteerSelfServiceViews.spec.ts`
  - 覆盖个人中心、报名记录、打卡记录、我的帖子、我的评论、我的收藏的 loading / empty / pagination / 操作按钮可见性。
- 已继续收敛共享显示映射：
  - `Frontend/src/views/content/ContentManageView.vue` 改为复用 `getForumPostStatusLabel`、`getForumPostStatusTag`、`getCommentTargetLabel`
  - 门户端与志愿者端剩余状态枚举、评论类型文案、时间展示方式继续下沉到共享显示工具。
- 已建立统一 UI 验收入口：
  - `Frontend/package.json` 新增 `visual:baseline`、`verify:ui`
  - `verify:ui` 串联 `build -> test:unit -> smoke:check -> smoke -> visual:baseline`
- 验证：
  - `npm run build` 通过
  - `npm run test:unit` 通过
  - `npm run smoke:check` 通过
  - `npm run verify:ui` 通过
- 下一步：
  - `E6` 继续处理测试与安全配置告警治理。

37. 统一风格下一轮细化 E6 完成（2026-03-13）
- 已治理以下测试与构建文件：
  - `backend/src/test/java/com/community/modules/activity/ActivityControllerContractIT.java`
  - `backend/src/test/java/com/community/modules/content/ContentControllerCommentIT.java`
  - `backend/src/test/java/com/community/modules/content/ContentControllerContractIT.java`
  - `backend/pom.xml`
- 已完成以下调整：
  - 控制器 `WebMvcTest` 统一替换为 `@MockitoBean`
  - 对需要的测试排除 `UserDetailsServiceAutoConfiguration`，避免默认 `inMemoryUserDetailsManager`
  - `maven-surefire-plugin` 接入 `byte-buddy-agent` 并补 `-Xshare:off`，消除 Mockito 动态加载 agent 的风险提示
- 已收敛的问题：
  - `MockBean` 弃用告警
  - Mockito self-attach / dynamic agent 告警
  - 测试上下文缺失 `UserDetailsService` Bean 的启动问题
- 验证：
  - `mvn -DskipTests compile` 通过
  - `mvn "-Dtest=ActivityControllerContractIT,ContentControllerContractIT,ContentControllerCommentIT" test` 通过，`14` 个测试全部通过

38. 下一轮结构治理切片 F1 完成（2026-03-13）
- 已新增内容模块查询服务：
  - `backend/src/main/java/com/community/modules/content/service/ContentQueryService.java`
- 已将以下查询路径从 `ContentService` 下沉到 `ContentQueryService`：
  - 动态分页
  - 热门动态 Top 5
  - 公告分页
  - 轮播图列表
  - 论坛分类列表
  - 论坛帖子分页
  - 评论分页
  - 我的收藏列表/分页
- `ContentService` 已改为“查询委托 + 写路径保留”的门面层，未改动 `ContentController` 和现有接口契约。
- 本轮结果：
  - `ContentService` 主文件体积已从原大体量查询/写混合结构收敛到以写路径为主
  - 内容模块后续可继续沿 `F2` 拆分命令职责
- 已新增下一轮结构治理任务单：
  - `file/16-结构治理下一轮切片任务清单.md`
- 验证：
  - `mvn -DskipTests compile` 通过
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT" test` 通过，`10` 个测试全部通过

39. 下一轮结构治理切片 F2 完成（2026-03-13）
- 已新增内容模块命令服务：
  - `backend/src/main/java/com/community/modules/content/service/ContentCommandService.java`
- `ContentService` 已进一步收敛为“查询委托 + 命令委托”门面层，读路径委托 `ContentQueryService`，写路径委托 `ContentCommandService`。
- 本轮拆分覆盖命令职责：
  - 动态、公告、轮播、论坛分类、帖子、评论、收藏的写入、更新、删除与级联清理
  - 活动存在性校验、评论目标校验、收藏优先级规整等领域校验
- 已补充最小命令回归测试：
  - `backend/src/test/java/com/community/modules/content/ContentCommandServiceTest.java`
  - 覆盖收藏幂等更新、他人帖子禁止编辑、非法评论目标拦截
- 验证：
  - `mvn -DskipTests compile` 通过
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test` 通过，`13` 个测试全部通过
- 下一步：
  - 进入 `F3`，继续收敛重型管理页共享骨架与状态表达。

40. 下一轮结构治理切片 F3 完成（2026-03-13）
- 已新增共享管理内容分区组件：
  - `Frontend/src/components/admin/AdminContentSection.vue`
- 已将以下重型管理页继续收敛到统一骨架：
  - `Frontend/src/views/activity/ActivityManageView.vue`
  - `Frontend/src/views/content/ContentManageView.vue`
- 本轮统一内容：
  - `ActivityManageView` 接入 `AdminListScaffold + AdminContentSection`，统一页头说明、操作区、加载态、空态与分页区域。
  - `ContentManageView` 接入 `AdminListScaffold + AdminContentSection`，四个标签页均具备独立分页、独立加载、独立刷新边界。
  - 翻页时同步清理勾选状态，避免跨页残留批量操作 ID。
- 已同步更新最小前端回归断言：
  - `Frontend/src/views/content/__tests__/ContentManageView.spec.ts`
  - 覆盖初始只加载当前标签页、帖子审核只刷新当前标签页、评论分页只触发当前标签页请求。
- 验证：
  - `npm run build` 通过
  - `npm run test:unit` 通过，`9` 个测试全部通过
  - `npm run smoke:check` 通过
- 下一步：
  - 进入 `F4`，扩展内容模块自动化回归。

41. 下一轮结构治理切片 F4 完成（2026-03-13）
- 已扩展内容模块后端接口契约回归：
  - `backend/src/test/java/com/community/modules/content/ContentControllerContractIT.java`
  - 新增首页聚合、动态分页、公告分页、动态创建、公告创建、帖子审核、帖子提交、收藏更新等契约断言。
- 已扩展内容模块前端页面交互回归：
  - `Frontend/src/views/content/__tests__/ContentManageView.spec.ts`
  - 新增动态保存后仅刷新当前标签页、公告批量删除后仅刷新当前标签页的断言。
- 本轮结果：
  - 内容模块查询与写路径的接口层门禁继续补齐，覆盖门户聚合查询、管理端内容写操作与志愿者侧内容提交路径。
  - 内容管理页已具备更完整的标签页级局部刷新增量回归，避免后续重构误伤非当前模块。
- 验证：
  - `mvn -DskipTests compile` 通过
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test` 通过，`21` 个测试全部通过
  - `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts` 通过，`5` 个测试全部通过
  - `npm run test:unit` 通过，`11` 个测试全部通过
  - `npm run build` 通过
  - `npm run smoke:check` 通过
- 当前状态：
  - `file/16-结构治理下一轮切片任务清单.md` 本轮 `F1-F4` 已全部完成。

42. 下一轮结构治理切片 G1 完成（2026-03-13）
- 已新增下一轮任务单：
  - `file/17-显式测试标记下一轮切片任务清单.md`
- 已完成评论测试数据显式标记治理：
  - `backend/src/main/resources/db/schema.sql`
  - `backend/src/main/resources/db/data.sql`
  - `backend/src/main/java/com/community/modules/content/controller/ContentController.java`
  - `backend/src/main/java/com/community/modules/content/service/ContentService.java`
  - `backend/src/main/java/com/community/modules/content/service/ContentQueryService.java`
  - `Frontend/src/utils/request.ts`
  - `Frontend/scripts/smoke-flow.mjs`
  - `Frontend/src/views/content/ContentManageView.vue`
- 本轮收敛内容：
  - 评论表新增 `test_data_tag`，替代旧的关键字式测试数据语义。
  - 评论创建仅从请求头 `X-Test-Data-Marker` 写入测试标记，普通业务入参不承担测试字段语义。
  - 门户端与志愿者端评论分页默认隔离测试数据，管理端评论页显式查看并标识“测试数据”。
  - 冒烟脚本结束时按测试标记清理评论，不再依赖评论文案删除。
- 已补充回归：
  - `backend/src/test/java/com/community/modules/content/ContentControllerContractIT.java`
  - `backend/src/test/java/com/community/modules/content/ContentControllerCommentIT.java`
  - `backend/src/test/java/com/community/modules/content/ContentCommandServiceTest.java`
  - `Frontend/src/views/content/__tests__/ContentManageView.spec.ts`
- 验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test`
  - `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts`
  - `npm run test:unit`
  - `npm run build`
  - `npm run smoke:check`

43. 下一轮结构治理切片 G2 完成（2026-03-13）
- 已完成收藏记录活动快照字段治理：
  - `backend/src/main/resources/db/schema.sql`
  - `backend/src/main/java/com/community/modules/content/entity/FavoriteActivity.java`
  - `backend/src/main/java/com/community/modules/content/service/ContentCommandService.java`
  - `Frontend/src/api/content.ts`
  - `Frontend/src/views/volunteer/VolunteerMyFavoritesView.vue`
- 本轮收敛内容：
  - 收藏表新增活动标题、地点、开始时间、结束时间快照字段。
  - 创建收藏时由后端固化快照，避免后续活动改名或改档期后直接影响历史收藏展示。
  - 初始化 SQL 会对旧收藏记录自动回填缺失快照，降低历史数据断层。
  - 收藏页改为优先展示快照信息，仍保留对当前活动数据的兜底回退。
- 已补充回归：
  - `backend/src/test/java/com/community/modules/content/ContentControllerContractIT.java`
  - `backend/src/test/java/com/community/modules/content/ContentCommandServiceTest.java`
  - `Frontend/src/views/volunteer/__tests__/VolunteerSelfServiceViews.spec.ts`
- 验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ContentControllerContractIT,ContentCommandServiceTest" test`
  - `npm run test:unit`
  - `npm run build`
  - `npm run smoke:check`

44. 下一轮结构治理切片 G3 完成（2026-03-13）
- 已完成管理端批量归档/恢复治理：
  - 活动模块新增独立 `ARCHIVED` 状态，避免继续复用 `ENDED` 表达归档
  - 管理端活动列表新增 `includeArchived=true` 查询能力，可显式查看已归档活动
  - 动态与公告模块复用现有 `status=1/0` 语义，补齐批量归档与恢复发布接口
- 已同步页面与显示层：
  - `Frontend/src/views/activity/ActivityManageView.vue` 新增批量归档、恢复发布操作
  - `Frontend/src/views/content/ContentManageView.vue` 新增动态/公告批量归档、恢复发布操作
  - `Frontend/src/utils/display.ts` 新增活动 `ARCHIVED` 状态标签与文案映射
- 已补充回归：
  - `backend/src/test/java/com/community/modules/activity/ActivityControllerContractIT.java`
  - `backend/src/test/java/com/community/modules/activity/ActivityCommandServiceTest.java`
  - `backend/src/test/java/com/community/modules/content/ContentControllerContractIT.java`
  - `backend/src/test/java/com/community/modules/content/ContentCommandServiceTest.java`
  - `Frontend/src/views/activity/__tests__/ActivityManageView.spec.ts`
  - `Frontend/src/views/content/__tests__/ContentManageView.spec.ts`
- 验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ActivityControllerContractIT,ActivityCommandServiceTest,ContentControllerContractIT,ContentCommandServiceTest" test`
  - `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts src/views/activity/__tests__/ActivityManageView.spec.ts`
  - `npm run test:unit`
  - `npm run build`
  - `npm run smoke:check`

45. 下一轮结构治理切片 G4 完成（2026-03-13）
- 已完成数据库初始化迁移治理基线：
  - `backend/pom.xml` 新增 `flyway-core`、`flyway-mysql`
  - `backend/src/main/resources/db/migration/V1__baseline_schema.sql` 承接当前基线结构
  - `backend/src/main/resources/db/migration/R__seed_reference_data.sql` 承接可重复应用的参考种子数据
- 已完成初始化边界收敛：
  - 应用启动不再依赖 `spring.sql.init`
  - `backend/src/main/resources/db/schema.sql`、`data.sql` 改为退役占位说明
  - `backend/src/main/resources/db/init-database.sql` 保留为建库建账号的手工引导脚本，不并入应用迁移链路
- 已完成配置治理：
  - `application.yml` 统一启用 `Flyway`，并配置 `baseline-on-migrate=true`、`baseline-version=0`
  - `application-dev.yml`、`application-prod.yml` 收敛为仅保留数据源配置
- 验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ActivityControllerContractIT,ContentControllerContractIT" test`
- 环境说明：
  - 本轮未直接启动应用对本地 MySQL 执行 live migration，避免未确认前改动现有开发库

46. G4 clone 库 live 验证与下一轮迁移治理规划完成（2026-03-13）
- 已完成真实数据库迁移验证：
  - 使用 `CVS` 克隆出 `CVS_FLYWAY_G4`
  - 在 clone 库上执行应用实际启动，确认 `Flyway` 可在非空库场景完成 baseline + versioned + repeatable 迁移
  - `flyway_schema_history` 已成功写入：
    - `0 << Flyway Baseline >>`
    - `1 baseline schema`
    - `seed reference data`
- 已确认当前迁移链路的两个剩余问题：
  - `V1__baseline_schema.sql` 会产生大量 `Table '...' already exists` 告警
  - `R__seed_reference_data.sql` 中 `VALUES(col)` 写法在 MySQL 8 已出现弃用告警
- 已新增下一轮任务单：
  - `file/18-数据库迁移治理下一轮切片任务清单.md`
- 下一步方向：
  - 继续按单切片方式推进数据库迁移治理，不扩散到新的业务功能
  - 下一片从 `H1` 开始，先治理非空库接入时的基线迁移噪音

47. H1 非空库基线迁移噪音治理完成（2026-03-13）
- 已完成 Flyway 基线迁移边界重构：
  - 新增 `backend/src/main/resources/db/migration/B1__baseline_schema.sql`，用于空库/新库初始化
  - 保留 `backend/src/main/resources/db/migration/V1__baseline_schema.sql`，仅用于兼容已落地的历史校验记录
  - 新增 `backend/src/main/resources/db/migration/V2__legacy_schema_alignment.sql`，用于旧库 baseline 接入后的兼容式列补齐与数据回填
  - `backend/src/main/resources/application.yml` 已将 `baseline-version` 从 `0` 调整为 `1`
- 已完成三类数据库验证：
  - `cvs_flyway_h1`：非空 clone 库首次接入成功，历史记录为 `baseline(1) -> V2 -> R`
  - `cvs_flyway_h1_empty`：空库初始化成功，历史记录为 `B1 -> V2 -> R`
  - `cvs_flyway_g4`：旧历史环境继续通过校验，并新增 `V2`
- 已确认本片效果：
  - 非空库接入时不再出现大规模 `Table '...' already exists` 告警
  - 当前数据库迁移剩余问题已收敛到参考数据迁移中的 MySQL 8 `VALUES(col)` 弃用告警
- 已同步任务单：
  - `file/18-数据库迁移治理下一轮切片任务清单.md` 中 `H1` 已标记完成

48. H2 参考数据迁移 MySQL 8 弃用语法治理完成（2026-03-13）
- 已完成参考数据迁移兼容性改造：
  - `backend/src/main/resources/db/migration/R__seed_reference_data.sql` 已将 `VALUES(col)` 全量替换为行别名写法 `VALUES (...) AS seed` + `seed.col`
  - `backend/src/main/resources/db/migration/README.md` 已同步注明 repeatable seed migration 不再依赖弃用语法
- 已完成三类数据库验证：
  - `cvs_flyway_h2`：非空 clone 库首次接入成功
  - `cvs_flyway_h2_empty`：空库初始化成功
  - `cvs_flyway_h1`：已有迁移历史库成功重跑 repeatable seed migration，并新增一条 repeatable 历史记录
- 已确认本片效果：
  - MySQL 8 `deprecated` 告警计数归零
  - `table already exists` 告警计数维持为零
- 已同步任务单：
  - `file/18-数据库迁移治理下一轮切片任务清单.md` 中 `H2` 已标记完成

49. H3 数据库迁移验收入口沉淀完成（2026-03-13）
- 已新增统一验收脚本：
  - `scripts/verify-flyway-clone.ps1`
- 脚本已串联以下步骤：
  - 自动打包 `backend` jar
  - 自动重建 clone 库
  - 自动执行 `mysqldump | mysql` 导入
  - 自动启动应用并生成日志
  - 自动输出 `flyway_schema_history` 与告警计数摘要
- 已同步更新：
  - `backend/src/main/resources/db/migration/README.md` 已新增统一验收命令与参数说明
- 已完成实跑验证：
  - 命令：`powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-clone.ps1 -SourceDatabase cvs -CloneDatabase cvs_flyway_h3b`
  - clone 库：`cvs_flyway_h3b`
  - 日志目录：`tmp/flyway-verify-20260313-175114`
- 结果：应用启动成功，`deprecated=0`，`already exists=0`
- 已同步任务单：
  - `file/18-数据库迁移治理下一轮切片任务清单.md` 中 `H3` 已标记完成

50. H4 数据库迁移部署与 DBA 边界文档收口完成（2026-03-13）
- 已完成迁移文档收口：
  - `backend/src/main/resources/db/migration/README.md`
  - `file/09-项目结构整理与优化报告.md`
  - `file/13-开发约束与计划跟踪.md`
- 已固定数据库职责边界：
  - `init-database.sql` 只负责建库、建账号、授权
  - `db/migration/B1__baseline_schema.sql` 只负责空库基线
  - `db/migration/V1__baseline_schema.sql` 只负责历史版本 `1` 校验兼容
  - `db/migration/V2__legacy_schema_alignment.sql` 只负责旧库接入后的对齐补丁
  - `db/migration/R__seed_reference_data.sql` 只负责可重复参考数据
- 已固定执行顺序：
  - 空库：`init-database.sql`（如需要）-> 应用启动 -> `B1 -> V2 -> R`
  - 非空旧库首次接入：备份或 clone -> 应用启动 -> `BASELINE(1) -> V2 -> R`
  - 已有 `V1` 历史环境：`validate -> pending V* -> pending R*`
- 已固定回滚口径：
  - 不再通过 `schema.sql` / `data.sql` 充当运行期入口或回滚入口
  - 结构/数据问题统一走“备份恢复”或“新增前向修复迁移”
- 当前结果：
  - `file/18-数据库迁移治理下一轮切片任务清单.md` 中 `H1-H4` 已全部完成

51. I1-A 角色边界与发布校验首片完成（2026-03-13）
- 已按“下一阶段功能补全”计划启动第一开发切片，先处理最容易返工的结构和校验问题。
- 已完成登录与路由边界收口：
  - 志愿者登录成功后默认进入 `/portal`
  - 管理员登录成功后默认进入 `/admin/dashboard`
  - 管理员带登录态访问 `/portal/*` 时会被强制重定向回后台
- 已完成后台与主页面入口隔离：
  - 管理端顶部不再显示“返回门户”
  - 管理员退出登录后返回登录页，不再回到门户
- 已完成活动管理新增/编辑交互改造：
  - `ActivityManageView` 已从右侧抽屉改为标准弹窗
- 已完成高风险发布链路空内容治理：
  - 管理端动态保存前校验标题和正文
  - 管理端公告保存前校验标题和正文
  - 志愿者发帖前校验标题、分类和正文
  - 后端 `ContentCommandService` 对动态、公告、帖子新增了服务层非空兜底校验
- 已补充并通过回归：
  - 前端构建 `npm run build`
  - 前端单测 `ContentManageView.spec.ts`、`ActivityManageView.spec.ts`
  - 前端脚本静态校验 `npm run smoke:check`
  - 后端测试 `mvn -Dtest=ContentCommandServiceTest test`
  - 后端编译 `mvn -DskipTests compile`


51. I1 数据库迁移回归矩阵完成（2026-03-13）
- 已新增数据库迁移公共脚本库：
  - `scripts/flyway-verify-lib.ps1`
- 已保持原有单场景 clone 验收入口兼容：
  - `scripts/verify-flyway-clone.ps1`
- 已新增三场景矩阵验收入口：
  - `scripts/verify-flyway-matrix.ps1`
- 已完成真实验证：
  - `powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-matrix.ps1 -SourceDatabase cvs -DatabasePrefix cvs_flyway_i1 -SkipPackage`
  - `powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-clone.ps1 -SourceDatabase cvs -CloneDatabase cvs_flyway_i1_cloneonly -SkipPackage`
- 三类场景结果：
  - `cvs_flyway_i1_empty`：空库初始化通过，`deprecated=0`，`already exists=0`
  - `cvs_flyway_i1_clone`：非空旧库首次接入通过，`deprecated=0`，`already exists=0`
  - `cvs_flyway_i1_history`：已有 `V1` 历史环境通过，`deprecated=0`，`already exists=0`
- 已新增可留档摘要：
  - `tmp/flyway-matrix-20260313-183947/matrix-summary.json`

52. I2 Flyway 变更准入守卫完成（2026-03-13）
- 已新增守卫脚本：
  - `scripts/check-flyway-guardrails.ps1`
- 已固定的治理检查：
  - 运行期初始化仍只能通过 Flyway 进入
  - `V1__baseline_schema.sql` 继续保持冻结
  - `B/V/R` 命名边界不能被打破
  - `R__seed_reference_data.sql` 不能漂移为结构迁移或 DBA SQL
  - `schema.sql`、`data.sql` 不能恢复为可执行入口
- 已完成验证：
  - `powershell -ExecutionPolicy Bypass -File scripts/check-flyway-guardrails.ps1`
  - 三组反例样本均被明确拦截

53. I3 前端 E2E / 冒烟回归补齐完成（2026-03-13）
- 已扩展现有 `Frontend/scripts/smoke-flow.mjs`，继续保持单脚本验收入口：
  - 活动批量归档、恢复发布
  - 动态批量归档、恢复发布
  - 公告批量归档、恢复发布
  - 收藏快照展示回归
  - 测试评论标记清理校验
  - 管理端批量删除反馈回归
- 已完成真实验证：
  - `npm run smoke:check`
  - `npm run smoke`
- 本轮实跑输出已确认：
  - `favorite-snapshot: verified`
  - `activity-archive/activity-restore: success`
  - `dynamic-archive/dynamic-restore: success`
  - `notice-archive/notice-restore: success`
  - `comment-cleanup: verified`
- 已同步更新：
  - `file/11-自动化冒烟脚本说明.md`
  - `file/12-阶段治理与迭代计划.md`
  - `file/13-开发约束与计划跟踪.md`
  - `file/19-数据库迁移回归矩阵下一轮切片任务清单.md`
