# 阶段治理与迭代计划

更新时间：2026-03-13

## 1. 当前判断

项目已经具备稳定的前后端分离主链路，原始规格任务与最近四轮补强切片均已完成。当前阶段已进入 `file/19` 的数据库迁移回归矩阵补强，重点从“迁移边界文档收口”推进到“迁移回归能力固化”：

- 回归能力：`I1` 已新增三场景数据库迁移回归矩阵入口，覆盖空库初始化、非空旧库首次接入、已有 `V1` 历史环境三类场景。
- 兼容结果：三类场景启动日志中的 `deprecated` 与 `already exists` 告警均为 `0`。
- 当前剩余重点：继续执行 `I3` 的前端 E2E / 冒烟回归补齐，完成 `file/19` 本轮收口。

## 2. 本轮目标

1. 在数据库守卫已固定的基础上，继续补齐前端关键链路的 E2E / 冒烟回归。
2. 保持前后端分离与统一验证入口，把本轮 `file/19` 剩余范围收敛到 `I3`。

## 2.1 后续默认开发口径

- 以正式 requirements / design 为唯一实现依据，按功能切片逐段推进。
- 每一段只做一个明确目标：实现 -> 验证 -> 文档更新 -> 进度汇报 -> 用户确认。
- 严格保持前后端分离与统一 UI 设计语言，不做跨层混写。
- 优先使用少量高质量 skill，必要时再补装，不做工具堆积。
- 持续做目录治理与代码瘦身，避免结构继续膨胀。

## 3. 分阶段执行

### 阶段 A：基线迁移噪音治理

- 复核 `V1__baseline_schema.sql` 在非空库接入场景下的执行行为。
- 明确“新库建表”和“旧库 baseline 接入”两条路径的真实边界，避免继续接受成功但高噪音的启动结果。

### 阶段 B：种子迁移兼容性治理

- 逐条处理 `R__seed_reference_data.sql` 中的 MySQL 8 弃用语法。
- 保持参考数据重复执行幂等，不把种子数据回退为一次性初始化脚本。

### 阶段 C：迁移验收入口沉淀

- 固化 clone 库创建、导入、启动验证与 `flyway_schema_history` 检查步骤。
- 让数据库迁移验收具备统一入口，避免依赖聊天上下文和人工记忆。

### 阶段 D：部署边界与文档收口

- 收敛 `init-database.sql` 与 `db/migration/*` 的职责说明、执行顺序和环境要求。
- 更新阶段治理与平台记录，确保下一轮数据库切片的执行顺序和退出条件清晰可追踪。
- 当前状态：已完成，文档已同步到迁移 README、结构报告和开发跟踪文档。

## 4. 需求驱动说明

- 历史文档多次引用 `.spec-workflow/specs/community-volunteer-service-platform/requirements.md`，本轮已将该正式需求基线恢复到工作树中。
- 当前 requirements、design、tasks 审批均已通过并归档。
- 当前 `community-volunteer-service-platform` 正式任务 `1-12` 已全部完成。
- 当前 `G1-G4` 补强切片已完成，后续治理任务以 `file/15` 至 `file/18` 的切片任务单为执行入口，不再回写旧 spec task id。
- 当前数据库迁移治理直接对齐 `design.md` 中的 Governance and Delivery Module 与 Testing Strategy，重点落实“数据库状态一致性”和“可重复验证”要求。

## 5. 当前核心 skill 策略

- 默认优先：`playwright`
- 按需启用：`playwright-interactive`、`doc`、`pdf`
- 原则：优先小而精的 skill 组合，只有在能明显降低交付风险或重复劳动时才新增 skill

## 6. 2026-03-13 执行结果

- 已删除根目录重复模板副本 `photo-organizer/` 和 `zero/`。
- 已删除根目录并行 `specs/` 非正式草案链路，避免与正式规格入口并存。
- 已创建正式需求基线：`.spec-workflow/specs/community-volunteer-service-platform/requirements.md`
- 已建立本地审批工具：`scripts/local-approval.ps1`
- 已创建 requirements 本地审批：`local_approval_20260313010858606_pxfklr6z`
- 已完成 requirements 审批通过并归档。
- 已创建设计文档：`.spec-workflow/specs/community-volunteer-service-platform/design.md`
- 已创建 design 本地审批：`local_approval_20260313080841916_pkht1nuo`
- 已完成 design 审批通过并归档。
- 已创建 tasks 文档：`.spec-workflow/specs/community-volunteer-service-platform/tasks.md`
- 已创建 tasks 本地审批：`local_approval_20260313081633315_uqictn9b`
- 已完成 tasks 审批通过并归档。
- 已进入 implementation 阶段并完成任务 1：
  - 后端统一登录失败提示（账号/密码/登录入口）
  - 前端新增角色归一化与登录后角色一致性兜底校验
  - 路由守卫新增非法 token+role 组合清理
  - 已记录 implementation log（taskId=1）
- 已完成任务 2（活动分类删除约束回归）：
  - 新增 `ActivityServiceCategoryTest` 覆盖分类被引用不可删、可删、批量去重、批量异常中断场景
  - `ActivityService.batchDeleteCategories` 增加 `distinct()` 去重处理
  - 已记录 implementation log（taskId=2）
  - 已通过验证：`mvn -Dtest=ActivityServiceCategoryTest test`
- 已完成任务 3（活动查询职责拆分）：
  - 新增 `ActivityQueryService`，承接活动分页/详情、报名分页、签到记录分页查询
  - `ActivityService` 查询方法改为统一委托 `ActivityQueryService`
  - 已记录 implementation log（taskId=3）
  - 已通过验证：`mvn -DskipTests compile`
- 已完成任务 4（活动命令职责拆分）：
  - 新增 `ActivityCommandService`，承接活动写路径业务规则
  - `ActivityService` 写路径改为统一委托 `ActivityCommandService`
  - 已记录 implementation log（taskId=4）
  - 已通过验证：`mvn -DskipTests compile`、`mvn -Dtest=ActivityServiceCategoryTest test`
- 已完成任务 5（论坛审核边界细化）：
  - 新增 `ForumModerationService`，承接帖子审核状态机与审核意见校验
  - `ContentService.auditPost` 改为委托 `ForumModerationService`
  - 已记录 implementation log（taskId=5）
  - 已通过验证：`mvn -DskipTests compile`
- 已完成任务 6（管理端剩余操作反馈链路统一）：
  - `ApplicationAuditView` 审核动作改为共享确认工具驱动
  - `VolunteerActivityCenterView` 的报名/收藏改为共享确认工具驱动并在成功后刷新列表
  - `runConfirmedAction` 支持自定义确认按钮文案与提示类型
  - 已记录 implementation log（taskId=6）
  - 已通过验证：`npm run build`
- 已完成任务 7（内容管理分页与按标签加载前端回归测试）：
  - 新增 `vitest + @vue/test-utils + jsdom` 前端单测基础设施
  - 新增 `ContentManageView.spec.ts`，覆盖首屏按标签懒加载与帖子审核后模块级刷新
  - 已记录 implementation log（taskId=7）
  - 已通过验证：`npm run test:unit -- ContentManageView.spec.ts`、`npm run test:unit`
- 已完成任务 8（评论分页与目标过滤后端集成测试）：
  - 新增 `ContentControllerCommentIT`，覆盖组合过滤正确性与匿名/登录态边界
  - `ContentController.pageComments` 新增 `targetId` 依赖 `targetType` 与 `onlyMine` 登录态校验
  - 已记录 implementation log（taskId=8）
  - 已通过验证：`mvn -Dtest=ContentControllerCommentIT test`
- 已完成任务 9（周榜重建与定时任务可观测性）：
  - `VolunteerWeeklyStatsService` 新增 trigger-aware 重建日志、读修复日志与事务保护
  - `ActivityScheduleTask` 增加定时任务开始/成功/失败日志与耗时字段
  - `DashboardController` 手动重建入口显式传递 `manual-api` 触发源
  - 已记录 implementation log（taskId=9）
  - 已通过验证：`mvn -DskipTests compile`
- 已完成任务 10（自动化冒烟扩展到审核与签到签退链路）：
  - `Frontend/scripts/smoke-flow.mjs` 新增临时活动预置、管理员审核、志愿者签到签退与周统计重建步骤
  - `Frontend/package.json` 新增 `npm run smoke:check`
  - `file/11-自动化冒烟脚本说明.md` 已同步刷新
  - 已记录 implementation log（taskId=10）
  - 已通过验证：`npm run smoke:check`
  - live 冒烟 `npm run smoke` 已尝试，但因本地后端未启动被环境阻塞
- 已完成任务 11（需求-接口映射清单）：
  - `file/02-接口文档.md` 新增 Requirement 1-6 总览矩阵
  - `file/08-新增接口与数据结构说明.md` 新增 Requirement -> 增量接口矩阵
  - 已记录 implementation log（taskId=11）
- 当前 `community-volunteer-service-platform` 规格任务 1-12 已全部完成，后续如有新范围变更，应新建下一轮规格而不是继续追加到本轮任务清单。
- 已新增 `Frontend/src/composables/useSelectionIds.ts` 与 `Frontend/src/utils/confirmed-action.ts`。
- 已将 `ActivityCategoryView`、`ActivityManageView`、`UserManageView`、`VolunteerMyFavoritesView` 的表格多选与危险操作确认逻辑收敛为共享能力。
- 已将 `VolunteerMyFavoritesView` 切换回 `useTable` 公共分页能力。
- 已将 `ContentManageView` 调整为按标签页加载与按模块刷新。
- 已刷新 `Frontend/package-lock.json` 顶层包名为 `zero2-frontend`。
- 已完成验证：
  - 前端 `npm run build`
  - 后端 `mvn -DskipTests compile`
  - 前端 `npm run test:unit`
  - 冒烟静态校验 `npm run smoke:check`
  - 冒烟脚本 `npm run smoke`（本轮尝试受本地后端未启动阻塞）
- 已启动下一轮“统一风格与前后端分离升级”切片，并完成 A1：
  - 新增 `Frontend/src/assets/styles/tokens.css`、`Frontend/src/assets/styles/index.css`
  - 样式入口切换为统一令牌基线，`theme.css` 改为消费设计令牌
  - `SearchForm.vue` 已接入共享圆角/间距变量
  - 已通过验证：`npm run build`
  - 页面快照验收尝试使用 Playwright，但本机 Chromium 安装超时，暂未完成浏览器层复核
- 已继续完成同一升级链路的 A3：
  - 新增 `Frontend/src/assets/styles/element-overrides.css`
  - 已将 Element Plus 主色、边框、表单、表格、分页、弹层样式统一映射到设计令牌
  - `index.css` 已固定为 `tokens -> element-overrides -> theme`
  - 已通过验证：`npm run build`
- 已继续完成同一升级链路的 A2：
  - `vite` 已升级到 `8.0.0`
  - `@vitejs/plugin-vue` 已升级到 `6.0.5`
  - 已刷新 `Frontend/package-lock.json`
  - 已通过验证：`npm run build`、`npm run test:unit`、`npm run smoke:check`
- 已进入 Milestone B，并完成 B1：
  - 新增 `Frontend/src/components/admin/AdminListScaffold.vue`
  - `SearchForm.vue` 已统一筛选区布局和操作区行为
  - `ActivityCategoryView`、`UserManageView`、`ApplicationAuditView` 已切换到共享页面骨架
  - 已通过验证：`npm run build`、`npm run test:unit`、`npm run smoke:check`
- 已继续完成同一升级链路的 B3：
  - 新增 `BrandMark.vue`、`WorkspaceHero.vue`、`StatePanel.vue` 三个共享组件
  - 门户端 `PortalNavBar` 与志愿者端 `MainLayout` 已统一品牌导航表达
  - 门户首页/活动/论坛/动态/公告与志愿者首页/活动中心/活动详情已统一页头层级、空态与加载态
  - 已通过验证：`npm run build`、`npm run test:unit`、`npm run smoke:check`
- 已回补完成同一升级链路的 B2：
  - `runConfirmedAction` 已支持 prompt 模式，统一覆盖“先输入理由再确认”的危险操作
  - `ApplicationAuditView` 与 `ContentManageView` 的拒绝审核已切换到共享确认工具
  - `VolunteerActivityDetailView` 的报名/收藏已与 `VolunteerActivityCenterView` 对齐为统一确认反馈
  - 已通过验证：`npm run build`、`npm run test:unit`、`npm run smoke:check`
- 已进入契约治理链路并完成 C1：
  - 后端新增 `ApiErrorCode`，统一业务错误码与 HTTP 状态映射
  - `BusinessException`、`GlobalExceptionHandler`、`SecurityConfig` 已收敛为真实状态码 + 标准错误码输出
  - 前端 `request.ts` 已统一错误码提示模板与登录失效处理
  - `ContentControllerCommentIT` 已更新为新的契约断言
  - 已通过验证：`mvn -Dtest=ContentControllerCommentIT test`、`mvn -DskipTests compile`、`npm run build`、`npm run test:unit`、`npm run smoke:check`

## 7. 2026-03-13 C2 契约一致性回归
- 已新增 `ActivityControllerContractIT`，固定活动分类列表、活动分页、活动详情、活动报名四类成功态契约。
- 已新增 `ContentControllerContractIT`，固定论坛分类列表、论坛帖子分页、我的收藏列表、收藏分页、收藏提交五类高频契约。
- 已复核 `Frontend/src/api/activity.ts` 与 `Frontend/src/api/content.ts`，当前前端分页类型 `PageResult<T>`、接口路径与请求层拆包方式无需额外修补。
- 已通过验证：`mvn -Dtest=ActivityControllerContractIT,ContentControllerContractIT,ContentControllerCommentIT test`、`mvn -DskipTests compile`、`npm run build`、`npm run test:unit`、`npm run smoke:check`

## 8. 2026-03-13 D1 自动化冒烟扩展
- 已将关键链路冒烟从“登录 / 报名 / 评论 / 退出”扩展到“登录 / 报名 / 评论 / 审核 / 签到 / 签退 / 周统计重建 / 批量删除 / 退出”。
- 本轮批量删除回归选定 `ActivityCategoryView`，覆盖统一列表骨架、多选列、确认弹窗与成功反馈。
- 冒烟脚本已补充临时活动与临时分类的自动清理，降低本地环境污染。
- 已完成真实链路验证：在本地启动前后端后，`npm run smoke` 全链路通过。

## 9. 2026-03-13 D2 UI 规范手册 PDF
- 已新增 `scripts/generate_ui_manual.py`，将统一风格手册生成流程沉淀为可重复执行脚本。
- 已输出 `file/14-1-UI规范手册.md` 与 `file/14-2-UI规范手册.pdf` 两份交付物，覆盖色板、字体层级、组件规范、页面模板、交互规则与响应式断点。
- PDF 生成流程已串联文本抽取校验与预览渲染校验，避免只生成文件而不验证内容完整性。
- 已通过验证：`python scripts/generate_ui_manual.py`

## 10. 2026-03-13 D3 验收报告与下一轮建议
- 已新增 `file/14-3-统一风格验收报告.md`，正式收口本轮统一风格升级验收。
- 报告已明确当前总体验收结论为“通过，附带环境说明”：
  - 编译、契约回归、前端构建、单测、静态冒烟校验、UI 手册生成均已通过
  - 当前环境未启动本地前后端，因此本轮未重跑 live 冒烟
- 已通过验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ActivityControllerContractIT,ContentControllerContractIT,ContentControllerCommentIT" test`
  - `npm run build`
  - `npm run test:unit`
  - `npm run smoke:check`
  - `python scripts/generate_ui_manual.py`

## 11. 2026-03-13 E1 志愿者长尾页统一收口
- 已新增 `Frontend/src/components/volunteer/VolunteerPageSection.vue`，统一志愿者自助页的内容卡片头部、操作区与分页区。
- 已完成志愿者长尾页收口：
  - `VolunteerProfileView`
  - `VolunteerApplyRecordsView`
  - `VolunteerCheckRecordsView`
  - `VolunteerMyFavoritesView`
  - `VolunteerMyPostsView`
  - `VolunteerMyCommentsView`
- 本轮同步补齐两项交互一致性：
  - `我的帖子` 与 `我的评论` 改为分页列表
  - `display.ts` 新增帖子状态与评论目标类型共享显示映射
- 已完成验证：
  - `mvn -DskipTests compile`
  - `npm run build`
  - `npm run test:unit`
  - `npm run smoke:check`
  - `npm run smoke`
- 已基于验收报告新增下一轮细化任务清单：
  - `file/15-统一风格下一轮细化任务清单.md`

## 12. 2026-03-13 E2-E5 统一风格验收链路补齐
- 已围绕门户端与志愿者端关键页面建立视觉快照基线，基线产物统一落盘到 `Frontend/output/playwright/baseline/`。
- 新增 `Frontend/scripts/visual-baseline.mjs`，补齐从 API 预置、登录态准备到截图输出的自动化视觉基线流程。
- 新增 `Frontend/src/views/volunteer/__tests__/VolunteerSelfServiceViews.spec.ts`，为志愿者长尾自助页补齐 loading / empty / pagination / 操作可见性的视图级断言。
- 收敛 `ContentManageView` 等页面的状态文案、评论类型文案与时间展示方式，减少页面层硬编码重复映射。
- 新增统一验收入口 `npm run verify:ui`，串联构建、单测、冒烟和视觉基线，避免后续只靠 live 手工验证。
- 本轮验证：
  - `npm run build`
  - `npm run test:unit`
  - `npm run smoke:check`
  - `npm run verify:ui`
- 下一步：继续执行 `E6`，处理测试与安全配置告警，补齐 `SecurityConfig` 相关治理。

## 13. 2026-03-13 E6 测试与安全配置告警治理
- 已完成测试配置治理：
  - 控制器测试统一将 `@MockBean` 迁移为 `@MockitoBean`
  - `WebMvcTest` 显式排除 `UserDetailsServiceAutoConfiguration`
  - surefire 接入 `byte-buddy-agent` 并补 `-Xshare:off`
- 已收敛的问题：
  - Spring Boot 对 `MockBean` 的弃用告警
  - Mockito dynamic agent / self-attach 告警
  - 测试上下文缺失 `UserDetailsService` Bean 的启动问题
- 本轮验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ActivityControllerContractIT,ContentControllerContractIT,ContentControllerCommentIT" test`
- 结果：`E1-E6` 已全部完成并完成统一风格阶段收口。

## 14. 2026-03-13 F1 内容模块查询职责拆分
- 已启动新一轮结构治理切片，并选定内容模块服务拆分作为首个后端治理项。
- 已新增 `ContentQueryService`，承接内容模块只读查询路径，避免 `ContentService` 同时承担过多查询职责。
- 本轮保持接口层不变：`ContentController` 无需改动，现有 `/api/content/*` 契约保持稳定。
- 已通过验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT" test`
- 下一步：继续执行 `file/16-结构治理下一轮切片任务清单.md` 中的 `F2`，拆分内容模块命令职责。

## 15. 2026-03-13 F2 内容模块命令职责拆分
- 已新增 `ContentCommandService`，承接内容模块写路径与领域校验，继续压缩 `ContentService` 的混合职责。
- `ContentService` 现已收敛为查询/命令双门面委托，`ContentController` 与现有 `/api/content/*` 契约保持不变。
- 新增 `ContentCommandServiceTest`，覆盖收藏幂等更新、他人帖子禁止编辑、非法评论目标拦截。
- 已通过验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test`
- 下一步：继续执行 `file/16-结构治理下一轮切片任务清单.md` 中的 `F3`。

## 16. 2026-03-13 F3 管理页共享骨架与状态表达收敛
- 已新增 `AdminContentSection`，统一管理页内部分区的标题说明、操作区、加载态、空态与分页区域。
- `ActivityManageView` 已接入统一管理页骨架，保持原有活动编辑抽屉与接口不变。
- `ContentManageView` 已收敛为按标签页独立分页、独立加载、独立刷新，避免重型页面每次操作都重载整页数据。
- 已补充 `ContentManageView` 的最小视图级回归断言，验证标签页级局部刷新与分页行为。
- 已通过验证：
  - `npm run build`
  - `npm run test:unit`
  - `npm run smoke:check`
- 下一步：继续执行 `file/16-结构治理下一轮切片任务清单.md` 中的 `F4`。

## 17. 2026-03-13 F4 内容模块自动化回归扩展
- 已扩展后端内容模块契约测试，补齐以下路径：
  - 首页聚合查询
  - 动态分页、公告分页
  - 动态创建、公告创建
  - 志愿者帖子提交、管理员帖子审核
  - 志愿者收藏更新
- 已扩展前端内容管理页回归，补齐以下行为：
  - 动态保存后仅刷新当前标签页
  - 公告批量删除后仅刷新当前标签页
- 已通过验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test`
  - `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts`
  - `npm run test:unit`
  - `npm run build`
  - `npm run smoke:check`
- 当前轮次结果：`file/16-结构治理下一轮切片任务清单.md` 中 `F1-F4` 已全部完成。

## 18. 2026-03-13 G1 评论测试数据显式标记
- 已新增 `file/17-显式测试标记下一轮切片任务清单.md`，进入下一轮结构治理切片。
- 已完成评论测试数据治理：
  - `comment_info` 新增 `test_data_tag`
  - 评论创建仅从请求头 `X-Test-Data-Marker` 接收测试标记
  - 普通评论分页默认隔离测试数据，管理端评论页显式带 `includeTestData=true` 查看与清理
- 已同步冒烟链路：
  - `Frontend/src/utils/request.ts` 自动透传本地测试标记请求头
  - `Frontend/scripts/smoke-flow.mjs` 自动写入测试标记并在结束时清理带标记评论
- 已补齐最小回归：
  - `ContentControllerContractIT`
  - `ContentControllerCommentIT`
  - `ContentCommandServiceTest`
  - `ContentManageView.spec.ts`
- 本轮验证目标：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test`
  - `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts`
  - `npm run test:unit`
  - `npm run build`
  - `npm run smoke:check`
- 下一步：保持单切片推进，进入 `file/17-显式测试标记下一轮切片任务清单.md` 中的 `G2`。

## 19. 2026-03-13 G2 收藏记录活动快照字段
- 已完成收藏快照字段治理：
  - `favorite_activity` 新增 `activity_title`、`activity_address`、`activity_start_time`、`activity_end_time`
  - 收藏创建时后端固化活动快照，旧收藏通过初始化 SQL 自动回填缺失快照
  - 收藏页展示优先使用快照，避免活动改名或改档期后直接污染历史收藏语义
- 已同步契约与前端模型：
  - `FavoriteActivity` / `FavoriteModel` 增加快照字段
  - `ContentControllerContractIT` 补齐收藏列表与分页的快照字段断言
  - `VolunteerSelfServiceViews.spec.ts` 补齐收藏页快照显示辅助逻辑断言
- 本轮验证目标：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ContentControllerContractIT,ContentCommandServiceTest" test`
  - `npm run test:unit`
  - `npm run build`
  - `npm run smoke:check`
- 下一步：继续执行 `file/17-显式测试标记下一轮切片任务清单.md` 中的 `G4`。

## 20. 2026-03-13 G3 管理端批量归档/恢复
- 已完成活动、动态、公告三类后台治理能力补齐：
  - 活动新增独立 `ARCHIVED` 状态，不再复用 `ENDED` 表达归档语义
  - 管理端活动分页支持 `includeArchived=true` 显式查看归档记录
  - 动态与公告复用现有发布状态 `1/0` 完成批量归档与恢复
- 已补齐接口与页面：
  - `ActivityController` / `ContentController` 新增批量归档、批量恢复接口
  - `ActivityManageView` / `ContentManageView` 新增批量归档、恢复发布操作入口
  - `display.ts` 新增活动 `ARCHIVED` 状态展示映射
- 已补齐最小回归：
  - `ActivityControllerContractIT`
  - `ActivityCommandServiceTest`
  - `ContentControllerContractIT`
  - `ContentCommandServiceTest`
  - `ActivityManageView.spec.ts`
  - `ContentManageView.spec.ts`
- 已通过验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ActivityControllerContractIT,ActivityCommandServiceTest,ContentControllerContractIT,ContentCommandServiceTest" test`
  - `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts src/views/activity/__tests__/ActivityManageView.spec.ts`
  - `npm run test:unit`
  - `npm run build`
  - `npm run smoke:check`
- 下一步：`file/17-显式测试标记下一轮切片任务清单.md` 中 `G1-G4` 已全部完成，等待下一轮切片确认。

## 21. 2026-03-13 G4 初始化 SQL 迁移治理预研
- 已完成数据库初始化链路治理：
  - 启动入口从 `spring.sql.init` 切换为 `Flyway`
  - 新增 `db/migration/V1__baseline_schema.sql`
  - 新增 `db/migration/R__seed_reference_data.sql`
  - 旧 `db/schema.sql`、`db/data.sql` 退役为占位说明，避免继续成为事实上的执行入口
- 已完成边界划分：
  - `init-database.sql` 保留为 DBA/本地环境的数据库与账号手工引导脚本
  - 应用运行期只负责 schema/data migration，不再负责建库建账号
  - 通过 `baseline-on-migrate=true` + `baseline-version=0` 兼容现有非空库接入
- 已完成配置与依赖：
  - `backend/pom.xml` 新增 `flyway-core`、`flyway-mysql`
  - `application.yml` 新增统一 `Flyway` 配置并关闭 `spring.sql.init`
  - `application-dev.yml`、`application-prod.yml` 收敛为仅保留数据源配置
- 已通过验证：
  - `mvn -DskipTests compile`
  - `mvn "-Dtest=ActivityControllerContractIT,ContentControllerContractIT" test`
- 环境说明：
  - 本轮未直接对本地 MySQL 执行 live 迁移启动验证，避免在未单独确认前对现有开发库做实际结构变更
- 下一步：当前 `G1-G4` 已全部完成，等待用户确认进入下一轮切片。

## 22. 2026-03-13 G4 clone 库 live 验证与下一轮数据库迁移治理
- 已完成真实数据库迁移验证：
  - 使用 clone 库 `CVS_FLYWAY_G4` 执行实际启动验证
  - `Flyway` 已成功创建 `flyway_schema_history`
  - 已确认三类记录均落表成功：
    - `0 << Flyway Baseline >>`
    - `1 baseline schema`
    - `seed reference data`（repeatable）
- 已确认当前迁移链路“可用但仍需治理”的两个具体问题：
  - `V1__baseline_schema.sql` 在非空库接入时输出大量 `Table '...' already exists` 告警
  - `R__seed_reference_data.sql` 中 `VALUES(col)` 在 MySQL 8 输出弃用告警
- 已新增下一轮任务单：
  - `file/18-数据库迁移治理下一轮切片任务清单.md`
- 下一轮执行原则：
  - 只围绕数据库迁移稳定性交付推进，不并发扩散到新的业务功能
  - 先处理 `H1`，再按顺序推进 `H2 -> H3 -> H4`
  - 每一片都需包含真实数据库验证或明确的可重复校验步骤

## 23. 2026-03-13 H1 非空库基线迁移噪音治理完成
- 已完成迁移边界重构：
  - 新增 `B1__baseline_schema.sql` 承接空库/新库建表基线
  - 保留 `V1__baseline_schema.sql` 不变，用于兼容已经落地的历史校验记录
  - 新增 `V2__legacy_schema_alignment.sql`，承接旧库接入时必须执行的兼容式列补齐与数据回填
  - `application.yml` 中 `baseline-version` 已调整为 `1`
- 已完成三类 live 验证：
  - `cvs_flyway_h1`：非空 clone 库首次接入，历史记录为 `baseline(1) -> V2 -> R`
  - `cvs_flyway_h1_empty`：空库初始化，历史记录为 `B1 -> V2 -> R`
  - `cvs_flyway_g4`：已落地旧历史环境，继续通过 `V1` 校验并新增 `V2`
- 已确认本片结果：
  - 非空库接入时不再出现大规模 `Table '...' already exists` 告警
  - 当前剩余数据库告警已收敛到 `R__seed_reference_data.sql` 的 `VALUES(col)` 弃用问题
- 下一步：继续执行 `H2`，只处理参考数据迁移中的 MySQL 8 弃用语法，不并发扩散到 `H3/H4`

## 24. 2026-03-13 H2 参考数据迁移 MySQL 8 弃用语法治理完成
- 已完成 `R__seed_reference_data.sql` 改造：
  - 所有 `ON DUPLICATE KEY UPDATE ... VALUES(col)` 已统一替换为 `VALUES (...) AS seed` + `seed.col`
  - `db/migration/README.md` 已同步注明 repeatable seed 迁移使用行别名，而不是已弃用的 `VALUES(col)`
- 已完成三类验证：
  - `cvs_flyway_h2`：非空 clone 库首次接入成功
  - `cvs_flyway_h2_empty`：空库初始化成功
  - `cvs_flyway_h1`：已有迁移历史库成功触发 repeatable 迁移重跑
- 已确认本片结果：
  - `deprecated` 告警计数为 `0`
  - `already exists` 告警计数维持为 `0`
  - 当前数据库迁移治理剩余范围已收缩到 `H3/H4`
- 下一步：继续执行 `H3`，沉淀可重复执行的数据库迁移验收入口

## 25. 2026-03-13 H3 数据库迁移验收入口沉淀完成
- 已新增统一验收脚本：
  - `scripts/verify-flyway-clone.ps1`
- 脚本当前能力：
  - 自动打包最新 `backend` jar
  - 自动重建 clone 库
  - 自动执行 `mysqldump | mysql` 导入
  - 自动启动应用并收集 `startup.out.log`
  - 自动输出 `flyway_schema_history`、`deprecated` 计数、`already exists` 计数、日志目录
- 已同步迁移说明：
  - `backend/src/main/resources/db/migration/README.md` 已新增统一验收命令和参数说明
- 已完成本机实跑验证：
  - 命令：`powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-clone.ps1 -SourceDatabase cvs -CloneDatabase cvs_flyway_h3b`
  - 结果：`cvs_flyway_h3b` 启动成功
  - 日志目录：`tmp/flyway-verify-20260313-175114`
  - 关键计数：`deprecated=0`、`already exists=0`
- 下一步：继续执行 `H4`，收口部署与 DBA 边界文档

## 26. 2026-03-13 H4 部署边界与文档收口完成
- 已完成以下文档更新：
  - `backend/src/main/resources/db/migration/README.md`
  - `file/09-项目结构整理与优化报告.md`
  - `file/13-开发约束与计划跟踪.md`
- 已固定迁移边界：
  - `init-database.sql` 只负责建库、建账号、授权
  - `B1/V1/V2` 只负责结构基线、历史兼容和旧库对齐
  - `R__seed_reference_data.sql` 只负责可重复参考数据
  - `schema.sql`、`data.sql` 继续作为退役占位说明，不再恢复为执行入口
- 已固定执行顺序：
  - 空库：`init-database.sql`（如需要）-> 应用启动 -> `B1 -> V2 -> R`
  - 非空旧库首次接入：备份或 clone -> 应用启动 -> `BASELINE(1) -> V2 -> R`
  - 已有 `V1` 历史环境：`validate -> pending V* -> pending R*`
- 已固定回滚口径：
  - 备份恢复
  - 新增前向修复迁移
- 当前状态：
  - `file/18-数据库迁移治理下一轮切片任务清单.md` 中 `H1-H4` 已全部完成
  - 下一轮应在用户确认后再新开切片


## 27. 2026-03-13 I1 数据库迁移回归矩阵完成
- 已新增数据库迁移公共脚本库：
  - `scripts/flyway-verify-lib.ps1`
- 已保持原有单场景入口兼容：
  - `scripts/verify-flyway-clone.ps1`
- 已新增三场景矩阵入口：
  - `scripts/verify-flyway-matrix.ps1`
- 已完成实跑验证：
  - `powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-matrix.ps1 -SourceDatabase cvs -DatabasePrefix cvs_flyway_i1 -SkipPackage`
  - `powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-clone.ps1 -SourceDatabase cvs -CloneDatabase cvs_flyway_i1_cloneonly -SkipPackage`
- 矩阵实跑结果：
  - 空库初始化：`cvs_flyway_i1_empty`，`deprecated=0`，`already exists=0`
  - 非空旧库首次接入：`cvs_flyway_i1_clone`，`deprecated=0`，`already exists=0`
  - 已有 `V1` 历史环境：`cvs_flyway_i1_history`，`deprecated=0`，`already exists=0`
- 已新增留档产物：
  - `tmp/flyway-matrix-20260313-183947/matrix-summary.json`
- 下一步：继续执行 `I2`，固化 Flyway 变更准入守卫。

## 28. 2026-03-13 I2 Flyway 变更准入守卫完成
- 已新增守卫脚本：
  - `scripts/check-flyway-guardrails.ps1`
- 已固定以下治理约束：
  - `spring.sql.init` 必须保持关闭
  - `spring.flyway.enabled=true`、`baseline-on-migrate=true`、`baseline-version=1` 必须保持不变
  - `V1__baseline_schema.sql` 必须保持冻结哈希
  - `db/migration/*.sql` 只能沿 `B/V/R` 命名边界扩展
  - `R__seed_reference_data.sql` 不能漂移为 DDL 或 DBA/bootstrap SQL
  - `schema.sql`、`data.sql` 不能恢复为运行期入口
- 已完成验证：
  - `powershell -ExecutionPolicy Bypass -File scripts/check-flyway-guardrails.ps1`
  - 反例样本 `sql-init-mode`、`v1-mutated`、`repeatable-ddl` 均被明确拦截
- 下一步：继续执行 `I3`，补齐前端 E2E / 冒烟回归。

## 29. 2026-03-13 I3 前端 E2E / 冒烟回归补齐完成
- 已在现有单入口脚本上完成回归扩展：
  - `Frontend/scripts/smoke-flow.mjs`
- 已补齐以下前端链路：
  - 管理端活动批量归档、恢复发布
  - 管理端动态批量归档、恢复发布
  - 管理端公告批量归档、恢复发布
  - 志愿者收藏快照展示回归
  - 测试评论标记清理后的显式校验
  - 管理端批量删除反馈回归
- 已完成真实验证：
  - `npm run smoke:check`
  - `npm run smoke`
- 本轮实跑结果：
  - 登录、报名、收藏、评论、审核、签到签退、周统计全部通过
  - `activity-archive/activity-restore` 通过
  - `dynamic-archive/dynamic-restore` 通过
  - `notice-archive/notice-restore` 通过
  - `comment-cleanup` 显式校验通过
- 已同步说明与跟踪：
  - `file/11-自动化冒烟脚本说明.md`
  - `file/13-开发约束与计划跟踪.md`
  - `file/06-平台完善记录.md`
- 当前状态：
  - `file/19-数据库迁移回归矩阵下一轮切片任务清单.md` 中 `I1-I3` 已全部完成
