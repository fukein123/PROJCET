# 结构治理下一轮切片任务清单

更新时间：2026-03-13

## Scope

- Baseline: `file/15-统一风格下一轮细化任务清单.md`
- Objective: 在统一风格与阶段治理闭环完成后，继续围绕“社区志愿服务管理”核心业务推进后端职责拆分与剩余结构治理，优先压缩大体量 Service 的职责边界。
- Current Status:
  - `F1` 至 `F4` 已完成

## Tasks

- [x] F1. 拆分内容模块查询职责
  - File: `backend/src/main/java/com/community/modules/content/service/ContentQueryService.java`
  - File: `backend/src/main/java/com/community/modules/content/service/ContentService.java`
  - 将动态、公告、轮播、论坛帖子、评论、收藏等查询逻辑下沉到 `ContentQueryService`，`ContentService` 保留写路径和门面委托。
  - Purpose: 收敛 `ContentService` 体积，继续落实 Requirement 6 的单一职责与服务分层约束。
  - Validation: `mvn -DskipTests compile`, `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT" test`

- [x] F2. 拆分内容模块命令职责
  - File: `backend/src/main/java/com/community/modules/content/service/ContentService.java`
  - File: `backend/src/main/java/com/community/modules/content/service/ContentCommandService.java`
  - File: `backend/src/test/java/com/community/modules/content/ContentCommandServiceTest.java`
  - 将动态、公告、轮播、评论、收藏等写路径继续按领域动作拆分，减少 `ContentService` 同时承载查询、校验、写入、级联删除的混合职责。
  - Purpose: 继续完成内容模块服务治理，为后续接口扩展与测试补齐打基础。
  - Validation: `mvn -DskipTests compile`, `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test`

- [x] F3. 收敛剩余重型页面的共享骨架与状态表达
  - File: `Frontend/src/views/activity/ActivityManageView.vue`
  - File: `Frontend/src/views/content/ContentManageView.vue`
  - File: `Frontend/src/components/admin/AdminContentSection.vue`
  - File: `Frontend/src/views/content/__tests__/ContentManageView.spec.ts`
  - 将尚未完全收敛到共享页面骨架的重型管理页面继续抽象，统一页头、操作区、空态、分页和局部刷新边界。
  - Purpose: 继续落实前端页面层复用治理，降低页面级重复结构。
  - Validation: `npm run build`, `npm run test:unit`, `npm run smoke:check`

- [x] F4. 扩展内容模块自动化回归
  - File: `backend/src/test/java/com/community/modules/content/*.java`
  - File: `Frontend/src/views/content/__tests__/*.spec.ts`
  - 为内容模块的剩余查询与写路径补齐回归测试，覆盖命令职责拆分后的关键接口与页面交互。
  - Purpose: 让内容模块后续重构继续具备稳定回归门禁。
  - Validation: `mvn -DskipTests compile`, `mvn "-Dtest=ContentControllerContractIT,ContentControllerCommentIT,ContentCommandServiceTest" test`, `npm run test:unit -- src/views/content/__tests__/ContentManageView.spec.ts`, `npm run test:unit`, `npm run build`, `npm run smoke:check`

## Execution Order

1. `F1 -> F2 -> F3 -> F4`

## Exit Criteria

- 内容模块查询职责从 `ContentService` 独立拆出。
- 内容模块写路径职责具备继续拆分的明确边界。
- 重型页面的共享骨架复用继续推进而不破坏前后端分离。
- 下一轮结构治理具备可重复执行的测试门禁。
