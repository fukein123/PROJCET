# 数据库迁移回归矩阵下一轮切片任务清单

更新时间：2026-03-13

## Scope

- Baseline: `file/18-数据库迁移治理下一轮切片任务清单.md`
- Objective: 在 `H1-H4` 已完成迁移边界治理后，继续围绕“社区志愿服务管理”交付稳定性，补齐数据库迁移三场景回归矩阵与后续准入守卫，优先把当前文档策略沉淀为可重复执行的验收能力。
- Design Alignment:
  - `.spec-workflow/specs/community-volunteer-service-platform/design.md`
  - Governance and Delivery Module
  - Testing Strategy 中的“数据库状态一致性”和“可重复验证”要求
- Validation Baseline:
  - 当前已有单场景验收入口：`scripts/verify-flyway-clone.ps1`
  - 当前已确认并文档化三类场景：
    - 空库初始化：`B1 -> V2 -> R`
    - 非空旧库首次接入：`BASELINE(1) -> V2 -> R`
    - 已有 `V1` 历史环境：`validate -> pending V* -> pending R*`
- 当前状态：`I1-I3` 已全部完成

## Tasks

- [x] I1. 建立三场景数据库迁移回归矩阵
  - File: `scripts/flyway-verify-lib.ps1`
  - File: `scripts/verify-flyway-clone.ps1`
  - File: `scripts/verify-flyway-matrix.ps1`
  - File: `backend/src/main/resources/db/migration/README.md`
  - File: `file/12-阶段治理与迭代计划.md`
  - File: `file/13-开发约束与计划跟踪.md`
  - 已将现有单场景 clone 验收脚本下沉公共逻辑，新增单命令矩阵入口，串联“空库初始化 / 非空旧库首次接入 / 已有 V1 历史环境”三类场景，统一输出 `flyway_schema_history`、启动日志告警计数与场景摘要。
  - Purpose: 把当前迁移治理从“文档正确”推进为“可回归、可复验、可留档”的工程能力。
  - Validation:
    - `powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-matrix.ps1 -SourceDatabase cvs -DatabasePrefix cvs_flyway_i1 -SkipPackage`
    - `powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-clone.ps1 -SourceDatabase cvs -CloneDatabase cvs_flyway_i1_cloneonly -SkipPackage`
    - 矩阵摘要：`tmp/flyway-matrix-20260313-183947/matrix-summary.json`

- [x] I2. 固化 Flyway 变更准入守卫
  - File: `scripts/check-flyway-guardrails.ps1`
  - File: `backend/src/main/resources/db/migration/README.md`
  - File: `file/12-阶段治理与迭代计划.md`
  - File: `file/13-开发约束与计划跟踪.md`
  - 已新增显式守卫脚本，固定 `spring.sql.init`、冻结迁移 `V1__baseline_schema.sql`、`B/V/R` 命名边界以及 `R__seed_reference_data.sql` 的职责范围。
  - Purpose: 把数据库迁移约束从“人工遵守”推进到“脚本可拦截”。
  - Validation:
    - `powershell -ExecutionPolicy Bypass -File scripts/check-flyway-guardrails.ps1`
    - 反例样本：`sql-init-mode`、`v1-mutated`、`repeatable-ddl` 三组 fixture 均被明确拦截

- [x] I3. 补齐前端 E2E 回归链路
  - File: `Frontend/scripts/`
  - File: `file/11-自动化冒烟脚本说明.md`
  - File: `file/12-阶段治理与迭代计划.md`
  - File: `file/13-开发约束与计划跟踪.md`
  - File: `file/06-平台完善记录.md`
  - 已沿用现有 `Frontend/scripts/smoke-flow.mjs` 单入口，补齐活动 / 动态 / 公告批量归档恢复、收藏快照展示、测试评论清理校验与管理端批量操作反馈回归。
  - Purpose: 补齐当前社区志愿服务管理主链路的前端自动化回归短板。
  - Validation:
    - `npm run smoke:check`
    - `npm run smoke`
    - 实跑结果已覆盖：登录、报名、收藏快照、评论、审核、签到签退、周统计、活动归档恢复、动态归档恢复、公告归档恢复、批量删除、退出、测试评论清理校验

## Execution Order

1. `I1 -> I2 -> I3`

## Exit Criteria

- 数据库迁移三类核心场景具备统一、可重复执行的验收入口。
- `flyway_schema_history`、启动告警计数与场景摘要可留档复核。
- 后续数据库改动具备明确准入守卫，不能轻易回退到隐式初始化模式。
- 前端关键管理与志愿者链路具备更完整的 E2E / 冒烟回归覆盖。
- 本轮 `I1-I3` 任务单已全部收口，可作为下一轮切片开发的验收基线。
