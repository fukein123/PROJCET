# 开发约束与计划跟踪

更新时间：2026-03-12

## 1. 当前阶段约束

### 1.1 需求阶段约束

- 当前已按用户明确要求恢复 `.spec-workflow` 流程，并从 `community-volunteer-service-platform` 的 `design` 阶段开始推进。
- `.spec-workflow/specs/community-volunteer-service-platform/requirements.md` 仍是当前需求基线，新增设计与任务文档必须严格对齐该文件。
- `design.md` 已创建并发起审批；在设计审批通过且清理审批记录前，不进入 `tasks` 和代码实现阶段。
- 当前 design 审批号：`approval_1773321302731_ruuvzv8i9`。

### 1.2 skill 使用约束

- 开发过程中优先使用 skill。
- 当前项目仓库内保留的核心 skill：
  - `frontend-design`
  - `webapp-testing`
  - `pdf`
- 如后续出现明确需求，再按需安装额外 skill。
- 当前本机缺少 `python`，`skill-installer` 自带脚本暂时无法直接执行；如果后续必须装新 skill，可以：
  - 先补齐 Python 环境后使用 `skill-installer`
  - 或采用手动下载/复制的替代方式

## 2. 已完成计划

- 已记录本机 Python 环境补装情况：
  - 用户已手动安装 Python 到 `F:\Python314`
  - 可执行文件验证通过：`F:\Python314\python.exe --version` = `Python 3.14.3`
- 已完成项目根目录、前后端目录、文档目录和本地 skill 目录盘点。
- 已完成前后端技术栈与需求文档对照，确认当前项目为前后端分离结构。
- 已完成前端重复逻辑收敛：
  - 统一分页查询逻辑
  - 统一状态展示与时间格式化
  - 统一图片上传校验
- 已完成明确废代码清理：
  - 删除未使用的 `Frontend/src/components/AppTable.vue`
  - 删除无效状态字段 `themeName`
- 已完成项目内 `skills/` 精简，只保留核心 skill。
- 已完成文档与部署配置中旧目录名 `admin/` 到 `Frontend/` 的修正。
- 已完成验证：
  - 前端 `npm run build`
  - 后端 `mvn -DskipTests compile`
  - 冒烟脚本 `npm run smoke`
- 已恢复 spec 工作流：
  - 已确认规格 `community-volunteer-service-platform` 当前状态为 `design-needed`
  - 已创建 `.spec-workflow/specs/community-volunteer-service-platform/design.md`
  - 已发起 design 审批，请求号 `approval_1773321302731_ruuvzv8i9`

## 3. 未完成计划

- 未完成 Python 命令别名与 PATH 收口。
  - 当前会话中的 `python` 仍优先指向 `C:\Users\lenovo\AppData\Local\Microsoft\WindowsApps\python.exe`
  - 如后续需要直接通过 `python` 调用 `skill-installer` 或其他脚本，需要再处理 PATH / App Execution Alias
- 未完成按需求文档逐项补足业务功能差距。
- 未完成 `community-volunteer-service-platform` 的 design 审批。
- 未完成 `community-volunteer-service-platform/tasks.md` 编写与审批。
- 未完成在 tasks 驱动下的正式代码实现与实现日志沉淀。
- 未完成后端进一步拆分大体量 Service，当前仍存在部分业务类偏重的问题。
- 未完成统一页面层更深层次的复用组件抽象，目前主要完成了逻辑层公共能力下沉。
- 未完成接口文档与需求文档逐条映射检查。
- 未完成测试体系补齐：
  - 后端自动化测试
  - 更完整的前端 E2E 回归

## 4. 暂不执行项

- 暂不在 design 审批完成前进入 `.spec-workflow` 的 `tasks` 阶段。
- 暂不在 `tasks.md` 获批前进入正式代码实现阶段。
- 暂不绕过审批工具接受口头/文本“已批准”结论。

## 5. 后续每轮执行规则

- 每一轮开始前，先查看本文件的“已完成计划”和“未完成计划”。
- 每一轮结束后，至少更新一次本文件和 `06-平台完善记录.md`。
- 如果本轮完成了某项未完成计划，要把它从“未完成计划”移动到“已完成计划”。
- 如果出现新的约束、阻塞或阶段切换，优先更新本文件，再继续开发。
