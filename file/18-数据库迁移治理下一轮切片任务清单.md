# 数据库迁移治理下一轮切片任务清单

更新时间：2026-03-13

## Scope

- Baseline: `file/17-显式测试标记下一轮切片任务清单.md`
- Objective: 在 `G4` 完成 `Flyway` 基线接入并通过 clone 库 live 验证后，继续围绕“社区志愿服务管理”交付稳定性推进数据库迁移治理，优先处理非空库接入噪音、MySQL 8 弃用语法与迁移验收入口缺失问题。
- Design Alignment:
  - `.spec-workflow/specs/community-volunteer-service-platform/design.md`
  - Governance and Delivery Module
  - Testing Strategy 中的“数据库状态一致性”和“可重复验证”要求
- Validation Baseline:
  - clone 库 `CVS_FLYWAY_G4` 已完成真实启动验证
  - `flyway_schema_history` 已成功写入 baseline / versioned / repeatable 记录
  - 当前状态：
    - `H1` 已完成：非空库接入时的 `Table '...' already exists` 告警已消失
    - `H2` 已完成：`R__seed_reference_data.sql` 中的 MySQL 8 `deprecated` 告警已消失
    - 当前剩余范围：无，本轮 `H1-H4` 已全部完成

## Tasks

- [x] H1. 收敛非空库接入时的基线迁移噪音
  - File: `backend/src/main/resources/db/migration/B1__baseline_schema.sql`
  - File: `backend/src/main/resources/db/migration/V1__baseline_schema.sql`
  - File: `backend/src/main/resources/db/migration/V2__legacy_schema_alignment.sql`
  - File: `backend/src/main/resources/application.yml`
  - File: `backend/src/main/resources/db/migration/README.md`
  - 基于 clone 库实测结果，将“空库建表基线”和“旧库对齐补丁”拆分为 `B1 + V2` 两条迁移路径：空库执行 `B1 -> V2`，非空库以 `baseline-version=1` 接入后只执行 `V2`，保留 `V1` 仅用于已落地历史的校验兼容。
  - Purpose: 保持 `Flyway` 接入稳定性，避免后续发布把“启动成功但告警泛滥”误判为可接受状态。
  - Validation: `mvn -DskipTests compile`、`mvn -DskipTests package`、`cvs_flyway_h1` live startup、`cvs_flyway_h1_empty` live startup、`cvs_flyway_g4` compatibility startup、检查 `flyway_schema_history` 和启动日志

- [x] H2. 清理参考数据迁移中的 MySQL 8 弃用语法
  - File: `backend/src/main/resources/db/migration/R__seed_reference_data.sql`
  - File: `backend/src/main/resources/db/migration/README.md`
  - 已将 `ON DUPLICATE KEY UPDATE` 中的 `VALUES(col)` 全量改写为 `VALUES (...) AS seed` + `seed.col` 行别名写法，保持参考数据重复执行时的幂等更新语义不变。
  - Purpose: 消除已确认的数据库弃用告警，避免未来数据库升级时把当前警告放大为迁移失败。
  - Validation: `mvn -DskipTests package`、`cvs_flyway_h2` live startup、`cvs_flyway_h2_empty` live startup、`cvs_flyway_h1` repeatable rerun startup、检查 `flyway_schema_history` 与启动日志中的 `deprecated/already exists`

- [x] H3. 建立可重复执行的数据库迁移验收入口
  - File: `scripts/verify-flyway-clone.ps1`
  - File: `backend/src/main/resources/db/migration/README.md`
  - 已将 clone 库创建、导入现有数据、应用启动验证、`flyway_schema_history` 检查等步骤沉淀为统一命令入口，默认串联 `mvn -DskipTests package -> DROP/CREATE clone DB -> mysqldump | mysql -> app startup -> warning/history summary`。
  - Purpose: 把本轮 live 验证从一次性动作沉淀为可重复执行的工程能力。
  - Validation: `powershell -ExecutionPolicy Bypass -File scripts/verify-flyway-clone.ps1 -SourceDatabase cvs -CloneDatabase cvs_flyway_h3b`；`cvs_flyway_h3b` 启动成功，`deprecated=0`，`already exists=0`，`flyway_schema_history` 写入 baseline / versioned / repeatable 记录

- [x] H4. 收口迁移部署与 DBA 边界文档
  - File: `backend/src/main/resources/db/migration/README.md`
  - File: `file/09-项目结构整理与优化报告.md`
  - File: `file/13-开发约束与计划跟踪.md`
  - 已明确 `init-database.sql`、`db/migration/V*.sql`、`db/migration/R*.sql` 的使用边界、执行顺序、环境要求与回滚约束，避免再次把建库建账号、基线迁移和种子数据混为一体。
  - Purpose: 为本地环境、测试环境和后续部署提供一致的数据库操作边界。
  - Validation: 文档复核通过，且与实际启动、clone 库验证步骤保持一致

## Execution Order

1. `H1 -> H2`
2. `H3 -> H4`

## Exit Criteria

- 非空库接入 `Flyway` 时不再产生大规模重复建表告警，或已有明确、可审计的策略说明。
- 参考数据迁移不再依赖 MySQL 8 弃用语法。
- 数据库迁移 live 验收具备统一、可重复执行的入口。
- `init-database.sql` 与 `db/migration/*` 的职责边界清晰，并已同步到治理文档。
